var ve = (t) => {
  throw TypeError(t);
};
var me = (t, e, r) => e.has(t) || ve("Cannot " + r);
var u = (t, e, r) => (me(t, e, "read from private field"), r ? r.call(t) : e.get(t)), _ = (t, e, r) => e.has(t) ? ve("Cannot add the same private member more than once") : e instanceof WeakSet ? e.add(t) : e.set(t, r), x = (t, e, r, s) => (me(t, e, "write to private field"), s ? s.call(t, r) : e.set(t, r), r), v = (t, e, r) => (me(t, e, "access private method"), r);
import { logger } from "@php-wasm/logger";
import { dirname, joinPaths, phpEventStdinTransfer, Semaphore, createSpawnHandler, splitShellCommand, basename, normalizePath, AcquireTimeoutError, resolvePathUnder } from "@php-wasm/util";
import { phpEventStdinTransfer as Ge } from "@php-wasm/util";
import { parse, stringify } from "ini";
import { StreamedFile } from "@php-wasm/stream-compression";
class ErrnoError extends Error {
  constructor(e, r, s) {
    super(r, s), this.name = "ErrnoError", this.errno = e;
  }
}
const FileErrorCodes = {
  0: "No error occurred. System call completed successfully.",
  1: "Argument list too long.",
  2: "Permission denied.",
  3: "Address in use.",
  4: "Address not available.",
  5: "Address family not supported.",
  6: "Resource unavailable, or operation would block.",
  7: "Connection already in progress.",
  8: "Bad file descriptor.",
  9: "Bad message.",
  10: "Device or resource busy.",
  11: "Operation canceled.",
  12: "No child processes.",
  13: "Connection aborted.",
  14: "Connection refused.",
  15: "Connection reset.",
  16: "Resource deadlock would occur.",
  17: "Destination address required.",
  18: "Mathematics argument out of domain of function.",
  19: "Reserved.",
  20: "File exists.",
  21: "Bad address.",
  22: "File too large.",
  23: "Host is unreachable.",
  24: "Identifier removed.",
  25: "Illegal byte sequence.",
  26: "Operation in progress.",
  27: "Interrupted function.",
  28: "Invalid argument.",
  29: "I/O error.",
  30: "Socket is connected.",
  31: "There is a directory under that path.",
  32: "Too many levels of symbolic links.",
  33: "File descriptor value too large.",
  34: "Too many links.",
  35: "Message too large.",
  36: "Reserved.",
  37: "Filename too long.",
  38: "Network is down.",
  39: "Connection aborted by network.",
  40: "Network unreachable.",
  41: "Too many files open in system.",
  42: "No buffer space available.",
  43: "No such device.",
  44: "There is no such file or directory OR the parent directory does not exist.",
  45: "Executable file format error.",
  46: "No locks available.",
  47: "Reserved.",
  48: "Not enough space.",
  49: "No message of the desired type.",
  50: "Protocol not available.",
  51: "No space left on device.",
  52: "Function not supported.",
  53: "The socket is not connected.",
  54: "Not a directory or a symbolic link to a directory.",
  55: "Directory not empty.",
  56: "State not recoverable.",
  57: "Not a socket.",
  58: "Not supported, or operation not supported on socket.",
  59: "Inappropriate I/O control operation.",
  60: "No such device or address.",
  61: "Value too large to be stored in data type.",
  62: "Previous owner died.",
  63: "Operation not permitted.",
  64: "Broken pipe.",
  65: "Protocol error.",
  66: "Protocol not supported.",
  67: "Protocol wrong type for socket.",
  68: "Result too large.",
  69: "Read-only file system.",
  70: "Invalid seek.",
  71: "No such process.",
  72: "Reserved.",
  73: "Connection timed out.",
  74: "Text file busy.",
  75: "Cross-device link.",
  76: "Extension: Capabilities insufficient."
};
function getEmscriptenFsError(t) {
  const e = typeof t == "object" ? t == null ? void 0 : t.errno : null;
  if (e in FileErrorCodes)
    return FileErrorCodes[e];
}
function rethrowFileSystemError(t = "") {
  return function(r) {
    return function(...s) {
      try {
        return r.apply(this, s);
      } catch (n) {
        const o = typeof n == "object" ? n == null ? void 0 : n.errno : null;
        if (o in FileErrorCodes) {
          const i = FileErrorCodes[o], a = typeof s[1] == "string" ? s[1] : null, c = a !== null ? t.replaceAll("{path}", a) : t;
          throw new ErrnoError(
            o,
            `${c}: ${i}`,
            {
              cause: n
            }
          );
        }
        throw n;
      }
    };
  };
}
class FSHelpers {
  /**
   * Reads a file from the PHP filesystem and returns it as a string.
   *
   * @throws {@link @php-wasm/universal:ErrnoError} – If the file doesn't exist.
   * @param FS
   * @param  path - The file path to read.
   * @returns The file contents.
   */
  static readFileAsText(e, r) {
    return new TextDecoder().decode(FSHelpers.readFileAsBuffer(e, r));
  }
  /**
   * Reads a file from the PHP filesystem and returns it as an array buffer.
   *
   * @throws {@link @php-wasm/universal:ErrnoError} – If the file doesn't exist.
   * @param FS
   * @param  path - The file path to read.
   * @returns The file contents.
   */
  static readFileAsBuffer(e, r) {
    return e.readFile(r);
  }
  /**
   * Overwrites data in a file in the PHP filesystem.
   * Creates a new file if one doesn't exist yet.
   *
   * @param FS
   * @param  path - The file path to write to.
   * @param  data - The data to write to the file.
   */
  static writeFile(e, r, s) {
    e.writeFile(r, s);
  }
  /**
   * Removes a file from the PHP filesystem.
   *
   * @throws {@link @php-wasm/universal:ErrnoError} – If the file doesn't exist.
   * @param FS
   * @param  path - The file path to remove.
   */
  static unlink(e, r) {
    e.unlink(r);
  }
  /**
   * Moves a file or directory in the PHP filesystem to a
   * new location.
   *
   * @param FS
   * @param fromPath The path to rename.
   * @param toPath The new path.
   */
  static mv(e, r, s) {
    try {
      const n = e.lookupPath(r).node.mount, o = FSHelpers.fileExists(e, s) ? e.lookupPath(s).node.mount : e.lookupPath(dirname(s)).node.mount;
      n.mountpoint !== o.mountpoint ? (FSHelpers.copyRecursive(e, r, s), FSHelpers.isDir(e, r) ? FSHelpers.rmdir(e, r, { recursive: !0 }) : e.unlink(r)) : e.rename(r, s);
    } catch (n) {
      const o = getEmscriptenFsError(n);
      throw o ? new Error(
        `Could not move ${r} to ${s}: ${o}`,
        {
          cause: n
        }
      ) : n;
    }
  }
  /**
   * Removes a directory from the PHP filesystem.
   *
   * @param FS
   * @param path The directory path to remove.
   * @param options Options for the removal.
   */
  static rmdir(e, r, s = { recursive: !0 }) {
    const n = e.lookupPath(r, { follow: !1 });
    if ((n == null ? void 0 : n.node.mount.mountpoint) === r)
      throw new ErrnoError(10);
    s != null && s.recursive && FSHelpers.listFiles(e, r).forEach((o) => {
      const i = `${r}/${o}`;
      FSHelpers.isDir(e, i) ? FSHelpers.rmdir(e, i, s) : FSHelpers.unlink(e, i);
    }), e.getPath(e.lookupPath(r).node) === e.cwd() && e.chdir(joinPaths(e.cwd(), "..")), e.rmdir(r);
  }
  /**
   * Lists the files and directories in the given directory.
   *
   * @param FS
   * @param  path - The directory path to list.
   * @param  options - Options for the listing.
   * @returns The list of files and directories in the given directory.
   */
  static listFiles(e, r, s = { prependPath: !1 }) {
    if (!FSHelpers.fileExists(e, r))
      return [];
    try {
      const n = e.readdir(r).filter(
        (o) => o !== "." && o !== ".."
      );
      if (s.prependPath) {
        const o = r.replace(/\/$/, "");
        return n.map((i) => `${o}/${i}`);
      }
      return n;
    } catch (n) {
      return logger.error(n, { path: r }), [];
    }
  }
  /**
   * Checks if a directory exists in the PHP filesystem.
   *
   * @param FS
   * @param  path – The path to check.
   * @returns True if the path is a directory, false otherwise.
   */
  static isDir(e, r) {
    return FSHelpers.fileExists(e, r) ? e.isDir(e.lookupPath(r, { follow: !0 }).node.mode) : !1;
  }
  /**
   * Checks if a file exists in the PHP filesystem.
   *
   * @param FS
   * @param  path – The path to check.
   * @returns True if the path is a file, false otherwise.
   */
  static isFile(e, r) {
    return FSHelpers.fileExists(e, r) ? e.isFile(e.lookupPath(r, { follow: !0 }).node.mode) : !1;
  }
  /**
   * Creates a symlink in the PHP filesystem.
   *
   * @param FS
   * @param target
   * @param link
   */
  static symlink(e, r, s) {
    return e.symlink(r, s);
  }
  /**
   * Checks if a path is a symlink in the PHP filesystem.
   *
   * @param FS
   * @param path
   * @returns True if the path is a symlink, false otherwise.
   */
  static isSymlink(e, r) {
    return FSHelpers.fileExists(e, r) ? e.isLink(e.lookupPath(r).node.mode) : !1;
  }
  /**
   * Reads the target of a symlink in the PHP filesystem.
   * @param FS
   * @param path
   * @returns The target of the symlink.
   * @throws {@link @php-wasm/universal:ErrnoError} – If the path is not a symlink.
   */
  static readlink(e, r) {
    return e.readlink(r);
  }
  /**
   * Gets the real path of a file in the PHP filesystem.
   * @param FS
   * @param path
   *
   * @returns The real path of the file.
   */
  static realpath(e, r) {
    return e.lookupPath(r, { follow: !0 }).path;
  }
  /**
   * Checks if a file (or a directory) exists in the PHP filesystem.
   *
   * @param FS
   * @param  path - The file path to check.
   * @returns True if the file exists, false otherwise.
   */
  static fileExists(e, r) {
    try {
      return e.lookupPath(r), !0;
    } catch {
      return !1;
    }
  }
  /**
   * Recursively creates a directory with the given path in the PHP filesystem.
   * For example, if the path is `/root/php/data`, and `/root` already exists,
   * it will create the directories `/root/php` and `/root/php/data`.
   *
   * @param FS
   * @param  path - The directory path to create.
   */
  static mkdir(e, r) {
    e.mkdirTree(r);
  }
  static copyRecursive(e, r, s) {
    try {
      const n = e.lookupPath(r).node;
      if (e.isDir(n.mode)) {
        if (r === s || s.startsWith(`${r}/`))
          throw new ErrnoError(28);
        e.mkdirTree(s);
        const o = e.readdir(r).filter(
          (i) => i !== "." && i !== ".."
        );
        for (const i of o)
          FSHelpers.copyRecursive(
            e,
            joinPaths(r, i),
            joinPaths(s, i)
          );
      } else e.isLink(n.mode) ? e.symlink(e.readlink(r), s) : e.writeFile(s, e.readFile(r));
    } catch (n) {
      const o = getEmscriptenFsError(n);
      throw o ? new Error(
        `Could not copy ${r} to ${s}: ${o}`,
        {
          cause: n
        }
      ) : n;
    }
  }
}
FSHelpers.readFileAsText = rethrowFileSystemError('Could not read "{path}"')(
  FSHelpers.readFileAsText
);
FSHelpers.readFileAsBuffer = rethrowFileSystemError('Could not read "{path}"')(
  FSHelpers.readFileAsBuffer
);
FSHelpers.writeFile = rethrowFileSystemError('Could not write to "{path}"')(
  FSHelpers.writeFile
);
FSHelpers.unlink = rethrowFileSystemError('Could not unlink "{path}"')(
  FSHelpers.unlink
);
FSHelpers.rmdir = rethrowFileSystemError('Could not remove directory "{path}"')(
  FSHelpers.rmdir
);
FSHelpers.listFiles = rethrowFileSystemError(
  'Could not list files in "{path}"'
)(FSHelpers.listFiles);
FSHelpers.isDir = rethrowFileSystemError('Could not stat "{path}"')(
  FSHelpers.isDir
);
FSHelpers.isFile = rethrowFileSystemError('Could not stat "{path}"')(
  FSHelpers.isFile
);
FSHelpers.realpath = rethrowFileSystemError('Could not stat "{path}"')(
  FSHelpers.realpath
);
FSHelpers.fileExists = rethrowFileSystemError('Could not stat "{path}"')(
  FSHelpers.fileExists
);
FSHelpers.mkdir = rethrowFileSystemError('Could not create directory "{path}"')(
  FSHelpers.mkdir
);
const _private = /* @__PURE__ */ new WeakMap();
var D, Y;
class PHPWorker {
  /** @inheritDoc */
  constructor(e, r) {
    _(this, D);
    _(this, Y);
    this.absoluteUrl = "", this.documentRoot = "", this.chroot = null, x(this, D, /* @__PURE__ */ new Map()), x(this, Y, /* @__PURE__ */ new WeakSet()), this.onMessageListeners = [], _private.set(this, {
      monitor: r
    }), e && this.__internal_setRequestHandler(e);
  }
  __internal_setRequestHandler(e) {
    this.absoluteUrl = e.absoluteUrl, this.documentRoot = e.documentRoot, this.chroot = this.documentRoot, _private.set(this, {
      ..._private.get(this),
      requestHandler: e
    });
  }
  /**
   * @internal
   * @deprecated
   * Do not use this method directly in the code consuming
   * the web API. It will change or even be removed without
   * a warning.
   */
  __internal_getPHP() {
    return _private.get(this).php;
  }
  /**
   * @internal
   * @deprecated
   * Do not use this method directly in the code consuming
   * the web API. It will change or even be removed without
   * a warning.
   */
  __internal_getRequestHandler() {
    return this.getRequestHandler();
  }
  async setPrimaryPHP(e) {
    _private.set(this, {
      ..._private.get(this),
      php: e
    });
  }
  /** @inheritDoc @php-wasm/universal!PHPRequestHandler.pathToInternalUrl  */
  pathToInternalUrl(e) {
    return this.getRequestHandler().pathToInternalUrl(e);
  }
  /** @inheritDoc @php-wasm/universal!PHPRequestHandler.internalUrlToPath  */
  internalUrlToPath(e) {
    return this.getRequestHandler().internalUrlToPath(e);
  }
  /**
   * The onDownloadProgress event listener.
   */
  async onDownloadProgress(e) {
    var r;
    return (r = _private.get(this).monitor) == null ? void 0 : r.addEventListener("progress", e);
  }
  /** @inheritDoc @php-wasm/universal!PHP.mv  */
  async mv(e, r) {
    return _private.get(this).php.mv(e, r);
  }
  /** @inheritDoc @php-wasm/universal!PHP.cp  */
  async cp(e, r) {
    return _private.get(this).php.cp(e, r);
  }
  /** @inheritDoc @php-wasm/universal!PHP.rmdir  */
  async rmdir(e, r) {
    return _private.get(this).php.rmdir(e, r);
  }
  /** @inheritDoc @php-wasm/universal!PHPRequestHandler.request */
  async request(e) {
    return await this.getRequestHandler().request(e);
  }
  /**
   * Handles a request with streaming support for large responses.
   * Returns a StreamedPHPResponse that allows processing the response
   * body incrementally without buffering the entire response in memory.
   *
   * This is useful for large file downloads (>2GB) that would otherwise
   * exceed JavaScript's Uint8Array size limits.
   *
   * @param request - PHP Request data.
   */
  async requestStreamed(e) {
    return await this.getRequestHandler().requestStreamed(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.run */
  async run(e) {
    const r = _private.get(this), s = r.php;
    if (!r.requestHandler && !(s != null && s.requestHandler) && s)
      return await s.run(e);
    const { php: n, reap: o } = await this.acquirePHPInstance();
    try {
      return await n.run(e);
    } finally {
      o();
    }
  }
  /**
   * Starts a PHP request and returns its output streams before PHP exits.
   *
   * A pooled PHP instance remains checked out until `response.finished`
   * settles. If the request fails before returning a response, the instance
   * is released immediately. A non-zero PHP exit is exposed through
   * `response.exitCode` instead of making this method reject.
   *
   * @param request - PHP code or script path, request metadata, and environment.
   * @returns A streamed response whose output can be consumed incrementally.
   * @throws When a PHP instance cannot be acquired or the request cannot start.
   */
  async runStream(e) {
    const r = _private.get(this), s = r.php;
    if (!r.requestHandler && !(s != null && s.requestHandler) && s)
      return await s.runStream(e);
    const { php: n, reap: o } = await this.acquirePHPInstance();
    let i;
    try {
      i = await n.runStream(e);
    } catch (a) {
      throw o(), a;
    }
    return i.finished.finally(o), i;
  }
  /** @inheritDoc @php-wasm/universal!/PHP.cli */
  async cli(e, r) {
    const s = _private.get(this), n = s.php;
    if (!s.requestHandler && !(n != null && n.requestHandler) && n)
      return await n.cli(e, r);
    const { php: o, reap: i } = await this.acquirePHPInstance();
    let a;
    try {
      a = await o.cli(e, r);
    } catch (c) {
      throw i(), c;
    }
    return a.finished.finally(i), a;
  }
  /** @inheritDoc @php-wasm/universal!/PHP.chdir */
  chdir(e) {
    return this.chroot = e, _private.get(this).php.chdir(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.chdir */
  cwd() {
    return _private.get(this).php.cwd();
  }
  /**
   * @returns A PHP instance with a consistent chroot.
   */
  async acquirePHPInstance() {
    const { php: e, reap: r } = await this.getRequestHandler().instanceManager.acquirePHPInstance();
    return this.chroot !== null && e.chdir(this.chroot), this.registerWorkerListeners(e), { php: e, reap: r };
  }
  /** @inheritDoc @php-wasm/universal!/PHP.setSapiName */
  setSapiName(e) {
    _private.get(this).php.setSapiName(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.mkdir */
  mkdir(e) {
    return _private.get(this).php.mkdir(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.mkdirTree */
  mkdirTree(e) {
    return _private.get(this).php.mkdirTree(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.readFileAsText */
  readFileAsText(e) {
    return _private.get(this).php.readFileAsText(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.readFileAsBuffer */
  readFileAsBuffer(e) {
    return _private.get(this).php.readFileAsBuffer(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.writeFile */
  writeFile(e, r) {
    return _private.get(this).php.writeFile(e, r);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.unlink */
  unlink(e) {
    return _private.get(this).php.unlink(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.listFiles */
  listFiles(e, r) {
    return _private.get(this).php.listFiles(e, r);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.isDir */
  isDir(e) {
    return _private.get(this).php.isDir(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.isFile */
  isFile(e) {
    return _private.get(this).php.isFile(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.fileExists */
  fileExists(e) {
    return _private.get(this).php.fileExists(e);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.onMessage */
  onMessage(e) {
    return this.onMessageListeners.push(e), async () => {
      this.onMessageListeners = this.onMessageListeners.filter(
        (r) => r !== e
      );
    };
  }
  /** @inheritDoc @php-wasm/universal!/PHP.defineConstant */
  defineConstant(e, r) {
    _private.get(this).php.defineConstant(e, r);
  }
  /** @inheritDoc @php-wasm/universal!/PHP.addEventListener */
  addEventListener(e, r) {
    u(this, D).has(e) || u(this, D).set(e, /* @__PURE__ */ new Set()), u(this, D).get(e).add(r);
  }
  /**
   * Removes an event listener for a PHP event.
   * @param eventType - The type of event to remove the listener from.
   * @param listener - The listener function to be removed.
   */
  removeEventListener(e, r) {
    var s;
    (s = u(this, D).get(e)) == null || s.delete(r);
  }
  dispatchEvent(e) {
    const r = u(this, D).get(e.type);
    if (!r)
      return;
    const s = [...r], n = phpEventStdinTransfer in e && e[phpEventStdinTransfer] === !0 && "stdin" in e && typeof ReadableStream < "u" && e.stdin instanceof ReadableStream;
    if (s.length > 1 && n) {
      let o = e.stdin;
      for (let i = 0; i < s.length - 1; i++) {
        const [a, c] = o.tee();
        o = c, s[i]({ ...e, stdin: a });
      }
      s[s.length - 1]({
        ...e,
        stdin: o
      });
      return;
    }
    for (const o of s)
      o(e);
  }
  registerWorkerListeners(e) {
    u(this, Y).has(e) || (u(this, Y).add(e), e.addEventListener("*", async (r) => {
      this.dispatchEvent(r);
    }), e.onMessage(async (r) => {
      for (const s of this.onMessageListeners) {
        const n = await s(r);
        if (n)
          return n;
      }
      return "";
    }));
  }
  async [Symbol.asyncDispose]() {
    var e;
    await ((e = this.getRequestHandler(!1)) == null ? void 0 : e[Symbol.asyncDispose]());
  }
  getRequestHandler(e = !0) {
    var s;
    const r = _private.get(this);
    if (r.requestHandler)
      return r.requestHandler;
    if ((s = r.php) != null && s.requestHandler)
      return this.__internal_setRequestHandler(r.php.requestHandler), r.php.requestHandler;
    if (e)
      throw new Error("PHPWorker is not connected to a request handler.");
  }
}
D = new WeakMap(), Y = new WeakMap();
function isExitCode(t) {
  return t instanceof Error ? (t == null ? void 0 : t.name) === "ExitStatus" && "status" in t : !1;
}
const RuntimeId = Symbol("RuntimeId"), loadedRuntimes = /* @__PURE__ */ new Map();
let lastRuntimeId = 0;
async function loadPHPRuntime(t, ...e) {
  const r = Object.assign({}, ...e), [s, n, o] = makePromise(), i = t.init(currentJsRuntime, {
    onAbort(l) {
      o(l), logger.error(l);
    },
    ENV: {},
    // Emscripten sometimes prepends a '/' to the path, which
    // breaks vite dev mode. An identity `locateFile` function
    // fixes it.
    locateFile: (l) => l,
    ...r,
    noInitialRun: !0,
    onRuntimeInitialized() {
      r.onRuntimeInitialized && r.onRuntimeInitialized(i), n();
    }
  });
  await s;
  const a = r.phpWasmAsyncMode ?? t.phpWasmAsyncMode;
  a && (i.phpWasmAsyncMode = a);
  const c = ++lastRuntimeId;
  return i.FS, i.id = c, i.originalExit = i._exit, i._exit = function(l) {
    return i.outboundNetworkProxyServer && (i.outboundNetworkProxyServer.close(), i.outboundNetworkProxyServer.closeAllConnections()), loadedRuntimes.delete(c), i.originalExit(l);
  }, i[RuntimeId] = c, loadedRuntimes.set(c, i), c;
}
function popLoadedRuntime(t, {
  dangerouslyKeepTheRuntimeInTheMap: e = !1
} = {}) {
  var s;
  const r = loadedRuntimes.get(t);
  if (!r)
    throw new Error(`Runtime with id ${t} not found`);
  if (e) {
    if (!((s = process == null ? void 0 : process.env) != null && s.TEST))
      throw new Error("Cannot pop runtime in non-test environment");
    return r;
  }
  return loadedRuntimes.delete(t), r;
}
const currentJsRuntime = function() {
  var t;
  return typeof process < "u" && ((t = process.release) == null ? void 0 : t.name) === "node" ? "NODE" : typeof window < "u" ? "WEB" : typeof WorkerGlobalScope < "u" && self instanceof WorkerGlobalScope ? "WORKER" : "NODE";
}(), makePromise = () => {
  const t = [], e = new Promise((r, s) => {
    t.push(r, s);
  });
  return t.unshift(e), t;
}, responseTexts = {
  500: "Internal Server Error",
  502: "Bad Gateway",
  404: "Not Found",
  403: "Forbidden",
  401: "Unauthorized",
  400: "Bad Request",
  301: "Moved Permanently",
  302: "Found",
  307: "Temporary Redirect",
  308: "Permanent Redirect",
  204: "No Content",
  201: "Created",
  200: "OK"
};
var re, se;
const de = class de {
  constructor(e, r, s, n) {
    /**
     * Headers stream that doesn't get locked when the consumer
     * reads the parsed headers. api.ts transfers the obj.getHeadersStream(),
     * and boot-playground-remote.ts copies the streamedResponse.headers.
     * Both streams must be readable when the StreamedPHPResponse is transferred
     * from the worker thread into the service worker.
     */
    _(this, re);
    /**
     * Headers stream reserved for internal parsing.
     */
    _(this, se);
    this.cachedParsedHeaders = null, this.cachedStdoutBytes = null, this.cachedStderrText = null;
    const [o, i] = e.tee();
    x(this, re, o), x(this, se, i), this.stdout = r, this.stderr = s, this.exitCode = n;
  }
  /**
   * Creates a StreamedPHPResponse from a buffered PHPResponse.
   * Useful for unifying response handling when both types may be returned.
   */
  static fromPHPResponse(e) {
    const r = new ReadableStream({
      start(a) {
        a.enqueue(e.bytes), a.close();
      }
    }), s = [];
    for (const [a, c] of Object.entries(e.headers))
      for (const l of c)
        s.push(`${a}: ${l}`);
    const n = JSON.stringify({
      status: e.httpStatusCode,
      headers: s
    }), o = new ReadableStream({
      start(a) {
        a.enqueue(new TextEncoder().encode(n)), a.close();
      }
    }), i = new ReadableStream({
      start(a) {
        e.errors.length > 0 && a.enqueue(
          new TextEncoder().encode(e.errors)
        ), a.close();
      }
    });
    return new de(
      o,
      r,
      i,
      Promise.resolve(e.exitCode)
    );
  }
  /**
   * Creates a StreamedPHPResponse for a given HTTP status code.
   * Shorthand for `StreamedPHPResponse.fromPHPResponse(PHPResponse.forHttpCode(...))`.
   */
  static forHttpCode(e, r = "") {
    return de.fromPHPResponse(
      PHPResponse.forHttpCode(e, r)
    );
  }
  /**
   * Returns the raw headers stream for serialization purposes.
   * For parsed headers, use the `headers` property instead.
   */
  getHeadersStream() {
    return u(this, re);
  }
  /**
   * True if the response is successful (HTTP status code 200-399),
   * false otherwise.
   */
  async ok() {
    try {
      const e = await this.httpStatusCode;
      return e >= 200 && e < 400;
    } catch {
      return !1;
    }
  }
  /**
   * Resolves when the response has finished processing – either successfully or not.
   */
  get finished() {
    return Promise.allSettled([this.exitCode.finally(() => {
    })]).then(
      () => {
      }
    );
  }
  /**
   * Resolves once HTTP headers are available.
   */
  get headers() {
    return this.getParsedHeaders().then((e) => e.headers);
  }
  /**
   * Resolves once HTTP status code is available.
   */
  get httpStatusCode() {
    return this.getParsedHeaders().then((e) => e.httpStatusCode).then((e) => e !== void 0 ? e : this.getParsedHeaders().then(
      (r) => r.httpStatusCode,
      () => 200
    )).catch(() => 500);
  }
  /**
   * Exposes the stdout bytes as they're produced by the PHP instance
   */
  get stdoutText() {
    return this.stdoutBytes.then(
      (e) => new TextDecoder().decode(e)
    );
  }
  /**
   * Exposes the stdout bytes as they're produced by the PHP instance
   */
  get stdoutBytes() {
    return this.cachedStdoutBytes || (this.cachedStdoutBytes = streamToBytes(this.stdout)), this.cachedStdoutBytes;
  }
  /**
   * Exposes the stderr bytes as they're produced by the PHP instance
   */
  get stderrText() {
    return this.cachedStderrText || (this.cachedStderrText = streamToText(this.stderr)), this.cachedStderrText;
  }
  async getParsedHeaders() {
    return this.cachedParsedHeaders || (this.cachedParsedHeaders = parseHeadersStream(
      u(this, se)
    )), await this.cachedParsedHeaders;
  }
};
re = new WeakMap(), se = new WeakMap();
let StreamedPHPResponse = de;
async function parseHeadersStream(t) {
  const e = await streamToText(t);
  let r;
  try {
    r = JSON.parse(e);
  } catch {
    return { headers: {}, httpStatusCode: 200 };
  }
  const s = {};
  for (const n of r.headers) {
    if (!n.includes(": "))
      continue;
    const o = n.indexOf(": "), i = n.substring(0, o).toLowerCase(), a = n.substring(o + 2);
    i in s || (s[i] = []), s[i].push(a);
  }
  return {
    headers: s,
    httpStatusCode: r.status
  };
}
async function streamToText(t) {
  const e = t.pipeThrough(new TextDecoderStream()).getReader(), r = [];
  for (; ; ) {
    const { done: s, value: n } = await e.read();
    if (s)
      return r.join("");
    n && r.push(n);
  }
}
async function streamToBytes(t) {
  const e = t.getReader(), r = [];
  for (; ; ) {
    const { done: s, value: n } = await e.read();
    if (s) {
      const o = r.reduce(
        (c, l) => c + l.byteLength,
        0
      ), i = new Uint8Array(o);
      let a = 0;
      for (const c of r)
        i.set(c, a), a += c.byteLength;
      return i;
    }
    n && r.push(n);
  }
}
class PHPResponse {
  constructor(e, r, s, n = "", o = 0) {
    this.httpStatusCode = e, this.headers = r, this.bytes = s, this.exitCode = o, this.errors = n;
  }
  static forHttpCode(e, r = "") {
    return new PHPResponse(
      e,
      {},
      new TextEncoder().encode(
        r || responseTexts[e] || ""
      )
    );
  }
  static fromRawData(e) {
    return new PHPResponse(
      e.httpStatusCode,
      e.headers,
      e.bytes,
      e.errors,
      e.exitCode
    );
  }
  static async fromStreamedResponse(e) {
    return await e.finished, new PHPResponse(
      await e.httpStatusCode,
      await e.headers,
      await e.stdoutBytes,
      await e.stderrText,
      await e.exitCode
    );
  }
  /**
   * True if the response is successful (HTTP status code 200-399),
   * false otherwise.
   */
  ok() {
    return this.httpStatusCode >= 200 && this.httpStatusCode < 400;
  }
  toRawData() {
    return {
      headers: this.headers,
      bytes: this.bytes,
      errors: this.errors,
      exitCode: this.exitCode,
      httpStatusCode: this.httpStatusCode
    };
  }
  /**
   * Response body as JSON.
   */
  get json() {
    return JSON.parse(this.text);
  }
  /**
   * Response body as text.
   */
  get text() {
    return new TextDecoder().decode(this.bytes);
  }
}
var _a;
const kError = Symbol("error"), kMessage = Symbol("message");
class ErrorEvent2 extends (_a = Event, _a) {
  /**
   * Create a new `ErrorEvent`.
   *
   * @param type The name of the event
   * @param options A dictionary object that allows for setting
   *                  attributes via object members of the same name.
   */
  constructor(e, r = {}) {
    super(e), this[kError] = r.error === void 0 ? null : r.error, this[kMessage] = r.message === void 0 ? "" : r.message;
  }
  get error() {
    return this[kError];
  }
  get message() {
    return this[kMessage];
  }
}
Object.defineProperty(ErrorEvent2.prototype, "error", { enumerable: !0 });
Object.defineProperty(ErrorEvent2.prototype, "message", { enumerable: !0 });
const ErrorEvent = typeof globalThis.ErrorEvent == "function" ? globalThis.ErrorEvent : ErrorEvent2;
class UnhandledRejectionsTarget extends EventTarget {
  constructor() {
    super(...arguments), this.listenersCount = 0;
  }
  addEventListener(e, r, s) {
    ++this.listenersCount, super.addEventListener(
      e,
      r,
      s
    );
  }
  removeEventListener(e, r, s) {
    --this.listenersCount, super.removeEventListener(
      e,
      r,
      s
    );
  }
  hasListeners() {
    return this.listenersCount > 0;
  }
}
function improveWASMErrorReporting(t) {
  const e = new UnhandledRejectionsTarget();
  for (const r in t.wasmExports)
    if (typeof t.wasmExports[r] == "function") {
      const s = t.wasmExports[r];
      t.wasmExports[r] = function(...n) {
        var o;
        try {
          return s(...n);
        } catch (i) {
          if (!(i instanceof Error))
            throw i;
          t.lastAsyncifyStackSource && (i.cause = t.lastAsyncifyStackSource);
          const a = clarifyErrorMessage(
            i,
            (o = t.lastAsyncifyStackSource) == null ? void 0 : o.stack
          );
          if (e.hasListeners()) {
            i.message = a;
            const c = new ErrorEvent("error", { error: i });
            throw e.dispatchEvent(c), i;
          }
          throw (!isExitCode(i) || i.status !== 0) && showCriticalErrorBox(a), i;
        }
      };
    }
  return e;
}
let functionsMaybeMissingFromAsyncify = [];
function getFunctionsMaybeMissingFromAsyncify() {
  return functionsMaybeMissingFromAsyncify;
}
function clarifyErrorMessage(t, e) {
  if (t.message === "unreachable") {
    let r = UNREACHABLE_ERROR;
    e || (r += `

This stack trace is lacking. For a better one initialize 
the PHP runtime with debug: true, e.g. loadNodeRuntime('8.1', { emscriptenOptions: { debug: true } }).

`);
    const s = new Set(
      extractPHPFunctionsFromStack(e || "")
    );
    let n = t;
    do {
      for (const o of extractPHPFunctionsFromStack(
        n.stack || ""
      ))
        s.add(o);
      n = n.cause;
    } while (n);
    functionsMaybeMissingFromAsyncify = Array.from(s);
    for (const o of s)
      r += `    * ${o}
`;
    return r += `Original error message: ${t.message}
`, r;
  }
  return t.message;
}
const UNREACHABLE_ERROR = `
"unreachable" WASM instruction executed.

The typical reason is a PHP function missing from the ASYNCIFY_ONLY
list when building PHP.wasm.

You will need to file a new issue in the WordPress Playground repository
and paste this error message there:

https://github.com/WordPress/wordpress-playground/issues/new

If you're a core developer, the typical fix is to:

* Isolate a minimal reproduction of the error
* Add a reproduction of the error to php-asyncify.spec.ts in the WordPress Playground repository
* Run 'npm run fix-asyncify'
* Commit the changes, push to the repo, release updated NPM packages

Below is a list of all the PHP functions found in the stack trace to
help with the minimal reproduction. If they're all already listed in
the Dockerfile, you'll need to trigger this error again with long stack
traces enabled. In node.js, you can do it using the --stack-trace-limit=100
CLI option: 

`, redBg = "\x1B[41m", bold = "\x1B[1m", reset = "\x1B[0m", eol = "\x1B[K";
let logged = !1;
function showCriticalErrorBox(t) {
  if (!logged && (logged = !0, !(t != null && t.trim().startsWith("Program terminated with exit")))) {
    logger.log(`${redBg}
${eol}
${bold}  WASM ERROR${reset}${redBg}`);
    for (const e of t.split(`
`))
      logger.log(`${eol}  ${e} `);
    logger.log(`${reset}`);
  }
}
function extractPHPFunctionsFromStack(t) {
  try {
    const e = t.split(`
`).slice(1).map((r) => {
      const s = r.trim().substring(3).split(" ");
      return {
        fn: s.length >= 2 ? s[0] : "<unknown>",
        isWasm: r.includes("wasm:/")
      };
    }).filter(
      ({ fn: r, isWasm: s }) => s && !r.startsWith("dynCall_") && !r.startsWith("invoke_")
    ).map(({ fn: r }) => r);
    return Array.from(new Set(e));
  } catch {
    return [];
  }
}
const STRING = "string", NUMBER = "number", __private__dont__use = Symbol("__private__dont__use");
class PHPExecutionFailureError extends Error {
  constructor(e, r, s) {
    super(e), this.response = r, this.source = s;
  }
}
class MountStillActiveError extends Error {
  constructor(e) {
    super("The filesystem could not be flushed and remains mounted.", {
      cause: e
    }), this.name = "MountStillActiveError";
  }
}
const PHP_INI_PATH = "/internal/shared/php.ini", AUTO_PREPEND_SCRIPT = "/internal/shared/auto_prepend_file.php", OPCACHE_FILE_FOLDER = "/internal/shared/opcache";
var Q, z, G, j, J, $, K, ne, k, y, ye, Ee, xe, Se, ke, Fe, be, Re, He, ge, Te, Pe, we;
class PHP {
  /**
   * Initializes a PHP runtime.
   *
   * @internal
   * @param  PHPRuntime - Optional. PHP Runtime ID as initialized by loadPHPRuntime.
   * @param  requestHandlerOptions - Optional. Options for the PHPRequestHandler. If undefined, no request handler will be initialized.
   */
  constructor(t) {
    _(this, y);
    _(this, Q);
    _(this, z, !1);
    _(this, G, null);
    _(this, j, /* @__PURE__ */ new Map([
      // Listen to all events
      ["*", /* @__PURE__ */ new Set()]
    ]));
    _(this, J, []);
    _(this, $, {});
    _(this, K);
    _(this, ne, /* @__PURE__ */ new Map());
    _(this, k, {
      enabled: !1,
      recreateRuntime: () => 0,
      needsRotating: !1,
      maxRequests: 400,
      requestsMade: 0
    });
    this.semaphore = new Semaphore({ concurrency: 1 }), t !== void 0 && this.initializeRuntime(t), this.addEventListener("request.error", (e) => {
      e.source === "php-wasm" && (u(this, k).needsRotating = !0);
    });
  }
  /**
   * Adds an event listener for a PHP event.
   * @param eventType - The type of event to listen for.
   * @param listener - The listener function to be called when the event is triggered.
   */
  addEventListener(t, e) {
    u(this, j).has(t) || u(this, j).set(t, /* @__PURE__ */ new Set()), u(this, j).get(t).add(e);
  }
  /**
   * Removes an event listener for a PHP event.
   * @param eventType - The type of event to remove the listener from.
   * @param listener - The listener function to be removed.
   */
  removeEventListener(t, e) {
    var r;
    (r = u(this, j).get(t)) == null || r.delete(e);
  }
  dispatchEvent(t) {
    const e = [
      ...u(this, j).get(t.type) || [],
      ...u(this, j).get("*") || []
    ];
    if (e.length === 0)
      return;
    const r = phpEventStdinTransfer in t && t[phpEventStdinTransfer] === !0 && "stdin" in t && typeof ReadableStream < "u" && t.stdin instanceof ReadableStream;
    if (e.length > 1 && r) {
      let s = t.stdin;
      for (let n = 0; n < e.length - 1; n++) {
        const [o, i] = s.tee();
        s = i, e[n]({ ...t, stdin: o });
      }
      e[e.length - 1]({
        ...t,
        stdin: s
      });
      return;
    }
    for (const s of e)
      s(t);
  }
  /**
   * Listens to message sent by the PHP code.
   *
   * To dispatch messages, call:
   *
   *     post_message_to_js(string $data)
   *
   *     Arguments:
   *         $data (string) – Data to pass to JavaScript.
   *
   * @example
   *
   * ```ts
   * const php = await PHP.load('8.0');
   *
   * php.onMessage(
   *     // The data is always passed as a string
   *     function (data: string) {
   *         // Let's decode and log the data:
   *         console.log(JSON.parse(data));
   *     }
   * );
   *
   * // Now that we have a listener in place, let's
   * // dispatch a message:
   * await php.run({
   *     code: `<?php
   *         post_message_to_js(
   *             json_encode([
   *                 'post_id' => '15',
   *                 'post_title' => 'This is a blog post!'
   *             ])
   *         ));
   *     `,
   * });
   * ```
   *
   * @param listener Callback function to handle the message.
   */
  onMessage(t) {
    return u(this, J).push(t), async () => {
      x(this, J, u(this, J).filter(
        (e) => e !== t
      ));
    };
  }
  async setSpawnHandler(handler) {
    typeof handler == "string" && (handler = createSpawnHandler(eval(handler))), x(this, K, handler);
  }
  /**
   * Overrides spawning of a specific binary, e.g. `sendmail`. The override
   * applies to any argv[0] whose basename matches `command` and takes
   * precedence over the handler installed via setSpawnHandler().
   */
  setCommandSpawnHandler(t, e) {
    u(this, ne).set(t, e);
  }
  /** @deprecated Use PHPRequestHandler instead. */
  get absoluteUrl() {
    return this.requestHandler.absoluteUrl;
  }
  /** @deprecated Use PHPRequestHandler instead. */
  get documentRoot() {
    return this.requestHandler.documentRoot;
  }
  /** @deprecated Use PHPRequestHandler instead. */
  pathToInternalUrl(t) {
    return this.requestHandler.pathToInternalUrl(t);
  }
  /** @deprecated Use PHPRequestHandler instead. */
  internalUrlToPath(t) {
    return this.requestHandler.internalUrlToPath(t);
  }
  initializeRuntime(t) {
    if (this[__private__dont__use])
      throw new Error("PHP runtime already initialized.");
    const e = popLoadedRuntime(t);
    if (!e)
      throw new Error("Invalid PHP runtime id.");
    if (this[__private__dont__use] = e, e.spawnProcess = (r, s, n) => v(this, y, ye).call(this, r, s, n), this[__private__dont__use].ccall(
      "wasm_set_phpini_path",
      null,
      ["string"],
      [PHP_INI_PATH]
    ), !this.fileExists(PHP_INI_PATH)) {
      const r = [
        // OPCache
        "opcache.enable = 1",
        "opcache.enable_cli = 1",
        "opcache.jit = 0",
        "opcache.interned_strings_buffer = 8",
        "opcache.max_accelerated_files = 1000",
        "opcache.memory_consumption = 64",
        "opcache.max_wasted_percentage = 5",
        "opcache.file_cache = " + OPCACHE_FILE_FOLDER,
        // Always enable the file cache.
        "opcache.file_cache_only = 1",
        "opcache.file_cache_consistency_checks = 1"
      ];
      this.fileExists(OPCACHE_FILE_FOLDER) || this.mkdir(OPCACHE_FILE_FOLDER), this.writeFile(
        PHP_INI_PATH,
        [
          "auto_prepend_file=" + AUTO_PREPEND_SCRIPT,
          "memory_limit=256M",
          "ignore_repeated_errors = 1",
          "error_reporting = E_ALL",
          "display_errors = 1",
          "html_errors = 1",
          "display_startup_errors = On",
          "log_errors = 1",
          "always_populate_raw_post_data = -1",
          "upload_max_filesize = 2000M",
          "post_max_size = 2000M",
          "allow_url_fopen = On",
          "allow_url_include = Off",
          "session.save_path = /home/web_user",
          "implicit_flush = 1",
          "output_buffering = 0",
          "max_execution_time = 0",
          "max_input_time = -1",
          ...r
        ].join(`
`)
      );
    }
    this.fileExists(AUTO_PREPEND_SCRIPT) || this.writeFile(
      AUTO_PREPEND_SCRIPT,
      `<?php
				// Define constants set via defineConstant() calls
				if(file_exists('/internal/shared/consts.json')) {
					$consts = json_decode(file_get_contents('/internal/shared/consts.json'), true);
					foreach ($consts as $const => $value) {
						if (!defined($const) && is_scalar($value)) {
							define($const, $value);
						}
					}
				}
				// Preload all the files from /internal/shared/preload
				foreach (glob('/internal/shared/preload/*.php') as $file) {
					require_once $file;
				}
				`
    ), e.onMessage = async (r) => {
      for (const s of u(this, J)) {
        const n = await s(r);
        if (n)
          return n;
      }
      return "";
    }, x(this, G, improveWASMErrorReporting(e)), this.dispatchEvent({
      type: "runtime.initialized"
    });
  }
  /** @inheritDoc */
  async setSapiName(t) {
    if (this[__private__dont__use].ccall(
      "wasm_set_sapi_name",
      NUMBER,
      [STRING],
      [t]
    ) !== 0)
      throw new Error(
        "Could not set SAPI name. This can only be done before the PHP WASM module is initialized.Did you already dispatch any requests?"
      );
    x(this, Q, t);
  }
  /**
   * Changes the current working directory in the PHP filesystem.
   * This is the directory that will be used as the base for relative paths.
   * For example, if the current working directory is `/root/php`, and the
   * path is `data`, the absolute path will be `/root/php/data`.
   *
   * @param  path - The new working directory.
   */
  chdir(t) {
    this[__private__dont__use].FS.chdir(t);
  }
  /**
   * Gets the current working directory in the PHP filesystem.
   *
   * @returns The current working directory.
   */
  cwd() {
    return this[__private__dont__use].FS.cwd();
  }
  /**
   * Changes the permissions of a file or directory.
   * @param path - The path to the file or directory.
   * @param mode - The new permissions.
   */
  chmod(t, e) {
    this[__private__dont__use].FS.chmod(t, e);
  }
  /**
   * Do not use. Use new PHPRequestHandler() instead.
   * @deprecated
   */
  async request(t) {
    if (logger.debug(
      "PHP.request() is deprecated. Please use new PHPRequestHandler() instead."
    ), !this.requestHandler)
      throw new Error("No request handler available.");
    return this.requestHandler.request(t);
  }
  /**
   * Runs PHP code.
   *
   * This low-level method directly interacts with the WebAssembly
   * PHP interpreter.
   *
   * Every time you call run(), it prepares the PHP
   * environment and:
   *
   * * Resets the internal PHP state
   * * Populates superglobals ($_SERVER, $_GET, etc.)
   * * Handles file uploads
   * * Populates input streams (stdin, argv, etc.)
   * * Sets the current working directory
   *
   * You can use run() in two primary modes:
   *
   * ### Code snippet mode
   *
   * In this mode, you pass a string containing PHP code to run.
   *
   * ```ts
   * const result = await php.run({
   * 	code: `<?php echo "Hello world!";`
   * });
   * // result.text === "Hello world!"
   * ```
   *
   * In this mode, information like `__DIR__` or `__FILE__` isn't very
   * useful because the code is not associated with any file.
   *
   * Under the hood, the PHP snippet is passed to the `zend_eval_string`
   * C function.
   *
   * ### File mode
   *
   * In the file mode, you pass a scriptPath and PHP executes a file
   * found at a that path:
   *
   * ```ts
   * await php.writeFile(
   * 	"/www/index.php",
   * 	`<?php echo "Hello world!";`
   * );
   * const result = await php.run({
   * 	scriptPath: "/www/index.php"
   * });
   * // result.text === "Hello world!"
   * ```
   *
   * In this mode, you can rely on path-related information like `__DIR__`
   * or `__FILE__`.
   *
   * Under the hood, the PHP file is executed with the `php_execute_script`
   * C function.
   *
   * The `run()` method cannot be used in conjunction with `cli()`.
   *
   * @example
   * ```js
   * const result = await php.run({
   * 	code: `<?php
   * 		$fp = fopen('php://stderr', 'w');
   * 		fwrite($fp, "Hello, world!");
   * 	`
   * });
   * // result.errors === "Hello, world!"
   * ```
   *
   * @deprecated Use stream() instead.
   * @param  request - PHP runtime options.
   */
  async run(t) {
    const e = await this.runStream(t), r = await PHPResponse.fromStreamedResponse(e);
    if (r.exitCode !== 0)
      throw new PHPExecutionFailureError(
        `PHP.run() failed with exit code ${r.exitCode}. 

=== Stdout ===
 ${r.text}

=== Stderr ===
 ${r.errors}`,
        r,
        "request"
      );
    return r;
  }
  /**
   * Runs PHP code and returns a StreamedPHPResponse object that can be used to
   * process the output incrementally.
   *
   * This low-level method directly interacts with the WebAssembly
   * PHP interpreter and provides streaming capabilities for processing
   * PHP output as it becomes available.
   *
   * Every time you call stream(), it prepares the PHP
   * environment and:
   *
   * * Resets the internal PHP state
   * * Populates superglobals ($_SERVER, $_GET, etc.)
   * * Handles file uploads
   * * Populates input streams (stdin, argv, etc.)
   * * Sets the current working directory
   *
   * You can use stream() in two primary modes:
   *
   * ### Code snippet mode
   *
   * In this mode, you pass a string containing PHP code to run.
   *
   * ```ts
   * const streamedResponse = await php.stream({
   * 	code: `<?php echo "Hello world!";`
   * });
   * // Process output incrementally
   * for await (const chunk of streamedResponse.text) {
   * 	console.log(chunk);
   * }
   * ```
   *
   * In this mode, information like `__DIR__` or `__FILE__` isn't very
   * useful because the code is not associated with any file.
   *
   * Under the hood, the PHP snippet is passed to the `zend_eval_string`
   * C function.
   *
   * ### File mode
   *
   * In the file mode, you pass a scriptPath and PHP executes a file
   * found at that path:
   *
   * ```ts
   * php.writeFile(
   * 	"/www/index.php",
   * 	`<?php echo "Hello world!";"`
   * );
   * const streamedResponse = await php.stream({
   * 	scriptPath: "/www/index.php"
   * });
   * // Process output incrementally
   * for await (const chunk of streamedResponse.text) {
   * 	console.log(chunk);
   * }
   * ```
   *
   * In this mode, you can rely on path-related information like `__DIR__`
   * or `__FILE__`.
   *
   * Under the hood, the PHP file is executed with the `php_execute_script`
   * C function.
   *
   * The `stream()` method cannot be used in conjunction with `cli()`.
   *
   * @example
   * ```js
   * const streamedResponse = await php.stream({
   * 	code: `<?php
   * 		for ($i = 0; $i < 5; $i++) {
   * 			echo "Line $i\n";
   * 			flush();
   * 		}
   * 	`
   * });
   *
   * // Process output as it becomes available
   * for await (const chunk of streamedResponse.text) {
   * 	console.log('Received:', chunk);
   * }
   *
   * // Get the final exit code
   * const exitCode = await streamedResponse.exitCode;
   * console.log('Exit code:', exitCode);
   * ```
   *
   * @see run() – a synchronous version of this method.
   * @param request - PHP runtime options.
   * @returns A StreamedPHPResponse object.
   */
  async runStream(t) {
    const e = await this.semaphore.acquire();
    let r;
    const s = v(this, y, we).call(this, async () => {
      if (u(this, z) || (await this[__private__dont__use].ccall(
        "php_wasm_init",
        null,
        [],
        [],
        {
          isAsync: !0
        }
      ), x(this, z, !0)), t.scriptPath && !this.fileExists(t.scriptPath))
        throw new Error(
          `The script path "${t.scriptPath}" does not exist.`
        );
      v(this, y, xe).call(this, t.relativeUri || ""), v(this, y, be).call(this, t.method || "GET");
      const o = normalizeHeaders(t.headers || {}), i = o.host || "example.com:443", a = v(this, y, Fe).call(this, i, t.protocol || "http");
      if (v(this, y, Se).call(this, i), v(this, y, ke).call(this, a), v(this, y, Re).call(this, o), t.body && (r = v(this, y, He).call(this, t.body)), typeof t.code == "string")
        this.writeFile("/internal/eval.php", t.code), v(this, y, ge).call(this, "/internal/eval.php");
      else if (typeof t.scriptPath == "string")
        v(this, y, ge).call(this, t.scriptPath || "");
      else
        throw new TypeError(
          "The request object must have either a `code` or a `scriptPath` property."
        );
      const c = v(this, y, Ee).call(this, t.$_SERVER, o, a);
      for (const d in c)
        v(this, y, Te).call(this, d, c[d]);
      const l = t.env || {};
      for (const d in l)
        v(this, y, Pe).call(this, d, l[d]);
      return await this[__private__dont__use].ccall(
        "wasm_sapi_handle_request",
        NUMBER,
        [],
        [],
        { async: !0 }
      );
    }), n = () => {
      if (r)
        try {
          this[__private__dont__use].free(r);
        } catch (o) {
          logger.error(o);
        }
      e(), this.dispatchEvent({
        type: "request.end"
      });
    };
    return s.then(
      (o) => (o.finished.finally(n), o),
      (o) => {
        try {
          n();
        } catch {
        } finally {
          throw o;
        }
      }
    );
  }
  /**
   * Defines a constant in the PHP runtime.
   * @param key - The name of the constant.
   * @param value - The value of the constant.
   */
  defineConstant(t, e) {
    let r = {};
    try {
      r = JSON.parse(
        this.fileExists("/internal/shared/consts.json") && this.readFileAsText("/internal/shared/consts.json") || "{}"
      );
    } catch {
    }
    this.writeFile(
      "/internal/shared/consts.json",
      JSON.stringify({
        ...r,
        [t]: e
      })
    );
  }
  /**
   * Recursively creates a directory with the given path in the PHP filesystem.
   * For example, if the path is `/root/php/data`, and `/root` already exists,
   * it will create the directories `/root/php` and `/root/php/data`.
   *
   * @param  path - The directory path to create.
   */
  mkdir(t) {
    const e = FSHelpers.mkdir(this[__private__dont__use].FS, t);
    return this.dispatchEvent({ type: "filesystem.write" }), e;
  }
  /**
   * @deprecated Use mkdir instead.
   */
  mkdirTree(t) {
    return FSHelpers.mkdir(this[__private__dont__use].FS, t);
  }
  /**
   * Reads a file from the PHP filesystem and returns it as a string.
   *
   * @throws {@link @php-wasm/universal:ErrnoError} – If the file doesn't exist.
   * @param  path - The file path to read.
   * @returns The file contents.
   */
  readFileAsText(t) {
    return FSHelpers.readFileAsText(this[__private__dont__use].FS, t);
  }
  /**
   * Reads a file from the PHP filesystem and returns it as an array buffer.
   *
   * @throws {@link @php-wasm/universal:ErrnoError} – If the file doesn't exist.
   * @param  path - The file path to read.
   * @returns The file contents.
   */
  readFileAsBuffer(t) {
    return FSHelpers.readFileAsBuffer(this[__private__dont__use].FS, t);
  }
  /**
   * Overwrites data in a file in the PHP filesystem.
   * Creates a new file if one doesn't exist yet.
   *
   * @param  path - The file path to write to.
   * @param  data - The data to write to the file.
   */
  writeFile(t, e) {
    const r = FSHelpers.writeFile(
      this[__private__dont__use].FS,
      t,
      e
    );
    return this.dispatchEvent({ type: "filesystem.write" }), r;
  }
  /**
   * Removes a file from the PHP filesystem.
   *
   * @throws {@link @php-wasm/universal:ErrnoError} – If the file doesn't exist.
   * @param  path - The file path to remove.
   */
  unlink(t) {
    const e = FSHelpers.unlink(this[__private__dont__use].FS, t);
    return this.dispatchEvent({ type: "filesystem.write" }), e;
  }
  /**
   * Moves a file or directory in the PHP filesystem to a
   * new location.
   *
   * @param fromPath The path to rename.
   * @param toPath The new path.
   */
  mv(t, e) {
    const r = FSHelpers.mv(
      this[__private__dont__use].FS,
      t,
      e
    );
    return this.dispatchEvent({ type: "filesystem.write" }), r;
  }
  /**
   * Copies a file or directory in the PHP filesystem to a
   * new location.
   *
   * @param fromPath The source path.
   * @param toPath The target path.
   */
  cp(t, e) {
    const r = FSHelpers.copyRecursive(
      this[__private__dont__use].FS,
      t,
      e
    );
    return this.dispatchEvent({ type: "filesystem.write" }), r;
  }
  /**
   * Removes a directory from the PHP filesystem.
   *
   * @param path The directory path to remove.
   * @param options Options for the removal.
   */
  rmdir(t, e = { recursive: !0 }) {
    const r = FSHelpers.rmdir(
      this[__private__dont__use].FS,
      t,
      e
    );
    return this.dispatchEvent({ type: "filesystem.write" }), r;
  }
  /**
   * Lists the files and directories in the given directory.
   *
   * @param  path - The directory path to list.
   * @param  options - Options for the listing.
   * @returns The list of files and directories in the given directory.
   */
  listFiles(t, e = { prependPath: !1 }) {
    return FSHelpers.listFiles(
      this[__private__dont__use].FS,
      t,
      e
    );
  }
  /**
   * Checks if a directory exists in the PHP filesystem.
   *
   * @param  path – The path to check.
   * @returns True if the path is a directory, false otherwise.
   */
  isDir(t) {
    return FSHelpers.isDir(this[__private__dont__use].FS, t);
  }
  /**
   * Checks if a file exists in the PHP filesystem.
   *
   * @param  path – The path to check.
   * @returns True if the path is a file, false otherwise.
   */
  isFile(t) {
    return FSHelpers.isFile(this[__private__dont__use].FS, t);
  }
  /**
   * Creates a symlink in the PHP filesystem.
   * @param target
   * @param path
   */
  symlink(t, e) {
    return FSHelpers.symlink(this[__private__dont__use].FS, t, e);
  }
  /**
   * Checks if a path is a symlink in the PHP filesystem.
   *
   * @param path
   * @returns True if the path is a symlink, false otherwise.
   */
  isSymlink(t) {
    return FSHelpers.isSymlink(this[__private__dont__use].FS, t);
  }
  /**
   * Reads the target of a symlink in the PHP filesystem.
   *
   * @param path
   * @returns The target of the symlink.
   */
  readlink(t) {
    return FSHelpers.readlink(this[__private__dont__use].FS, t);
  }
  /**
   * Resolves the real path of a file in the PHP filesystem.
   * @param path
   * @returns The real path of the file.
   */
  realpath(t) {
    return FSHelpers.realpath(this[__private__dont__use].FS, t);
  }
  /**
   * Checks if a file (or a directory) exists in the PHP filesystem.
   *
   * @param  path - The file path to check.
   * @returns True if the file exists, false otherwise.
   */
  fileExists(t) {
    return FSHelpers.fileExists(this[__private__dont__use].FS, t);
  }
  /**
   * Enables inline PHP runtime rotation after a certain number of requests
   * or an internal crash.
   */
  enableRuntimeRotation(t) {
    x(this, k, {
      ...u(this, k),
      enabled: !0,
      recreateRuntime: t.recreateRuntime,
      maxRequests: t.maxRequests ?? 400
    });
  }
  async rotateRuntime() {
    if (!u(this, k).enabled)
      throw new Error(
        "Runtime rotation is not enabled. Call enableRuntimeRotation() first."
      );
    await this.hotSwapPHPRuntime(
      await u(this, k).recreateRuntime()
    ), u(this, k).requestsMade = 0, u(this, k).needsRotating = !1;
  }
  /**
   * Hot-swaps the PHP runtime for a new one without
   * interrupting the operations of this PHP instance.
   *
   * @param runtime
   */
  async hotSwapPHPRuntime(t) {
    const e = this[__private__dont__use].FS, r = this.listFiles("/").map((a) => `/${a}`), s = e.cwd();
    e.chdir("/");
    const n = Object.entries(u(this, $)).map(
      ([a, c]) => ({
        mountHandler: c.mountHandler,
        mountPointSnapshot: snapshotMountPoint(e, a),
        vfsPath: a
      })
    ), o = Object.values(
      u(this, $)
    ).reverse();
    for (const a of o)
      await a.unmount();
    try {
      this.exit();
    } catch {
    }
    this.initializeRuntime(t), u(this, Q) && this.setSapiName(u(this, Q));
    const i = this[__private__dont__use].FS;
    for (const a of r)
      a && a !== "/request" && copyMEMFSNodes(e, i, a);
    for (const {
      mountHandler: a,
      mountPointSnapshot: c,
      vfsPath: l
    } of n)
      try {
        await this.mount(l, a);
      } catch (d) {
        if (isMissingMountSourceError(d)) {
          restoreMountPointSnapshot(
            i,
            l,
            c
          );
          continue;
        }
        if (!isMissingMountTargetPathError(d))
          throw d;
        this.mkdir(l), await this.mount(l, a);
      }
    try {
      i.chdir(s);
    } catch (a) {
      throw new Error(
        `Failed to restore CWD to ${s} after PHP runtime rotation.`,
        {
          cause: a
        }
      );
    }
  }
  /**
   * Mounts a filesystem to a given path in the PHP filesystem.
   *
   * The returned unmount function removes the mount from runtime-rotation
   * tracking on success or ordinary failure. `MountStillActiveError` leaves it
   * tracked because the handler guarantees the mount remains live and retryable.
   *
   * @param  virtualFSPath - Where to mount it in the PHP virtual filesystem.
   * @param  mountHandler - The mount handler to use.
   * @return Unmount function to unmount the filesystem.
   */
  async mount(t, e) {
    const r = await e(
      this,
      this[__private__dont__use].FS,
      t
    ), s = {
      mountHandler: e,
      unmount: async () => {
        try {
          await r();
        } catch (n) {
          throw n instanceof MountStillActiveError || delete u(this, $)[t], n;
        }
        delete u(this, $)[t];
      }
    };
    return u(this, $)[t] = s, () => s.unmount();
  }
  /**
   * Starts a PHP CLI session with given arguments.
   *
   * This method can only be used when PHP was compiled with the CLI SAPI
   * and it cannot be used in conjunction with `run()`.
   *
   * Once this method finishes running, the PHP instance is no
   * longer usable and should be discarded. This is because PHP
   * internally cleans up all the resources and calls exit().
   *
   * @param  argv - The arguments to pass to the CLI.
   * @returns The exit code of the CLI session.
   */
  async cli(t, e = {}) {
    if (basename(t[0] ?? "") !== "php")
      return this.subProcess(t, e);
    u(this, z) && (u(this, k).needsRotating = !0);
    const r = await this.semaphore.acquire();
    return await v(this, y, we).call(this, () => {
      const s = e.env || {};
      for (const [n, o] of Object.entries(s))
        v(this, y, Pe).call(this, n, o);
      t = [t[0], "-c", PHP_INI_PATH, ...t.slice(1)];
      for (const n of t)
        this[__private__dont__use].ccall(
          "wasm_add_cli_arg",
          null,
          [STRING],
          [n]
        );
      return this[__private__dont__use].ccall("run_cli", null, [], [], {
        async: !0
      });
    }).then((s) => (s.exitCode.finally(r), s)).finally(() => {
      u(this, k).needsRotating = !0;
    });
  }
  /**
   * Runs an arbitrary CLI command using the spawn handler associated
   * with this PHP instance.
   *
   * @param argv
   * @param options
   * @returns StreamedPHPResponse.
   */
  async subProcess(t, e = {}) {
    const r = v(this, y, ye).call(this, t[0], t.slice(1), {
      env: e.env,
      cwd: e.cwd ?? this.cwd()
    }), s = await createInvertedReadableStream();
    r.on("error", (a) => {
      safeStreamError$1(s.controller, a);
    });
    const n = (a) => {
      try {
        s.controller.enqueue(a);
      } catch {
        r.stderr.off("data", n);
      }
    };
    r.stderr.on("data", n);
    const o = await createInvertedReadableStream(), i = (a) => {
      try {
        o.controller.enqueue(a);
      } catch {
        r.stdout.off("data", i);
      }
    };
    return r.stdout.on("data", i), r.on("exit", () => {
      setTimeout(() => {
        try {
          s.controller.close();
        } catch {
        }
        try {
          o.controller.close();
        } catch {
        }
      }, 0);
    }), new StreamedPHPResponse(
      // Headers stream
      new ReadableStream({
        start(a) {
          a.close();
        }
      }),
      o.stream,
      s.stream,
      // Exit code
      new Promise((a) => {
        r.on("exit", (c) => {
          a(c);
        });
      })
    );
  }
  setSkipShebang(t) {
    this[__private__dont__use].ccall(
      "wasm_set_skip_shebang",
      null,
      [NUMBER],
      [t ? 1 : 0]
    );
  }
  exit(t = 0) {
    this.dispatchEvent({
      type: "runtime.beforeExit"
    });
    try {
      this[__private__dont__use]._exit(t);
    } catch {
    }
    x(this, z, !1), x(this, G, null), this[__private__dont__use] && (delete this[__private__dont__use].onMessage, delete this[__private__dont__use]);
  }
  [Symbol.dispose]() {
    this.exit(0);
  }
}
Q = new WeakMap(), z = new WeakMap(), G = new WeakMap(), j = new WeakMap(), J = new WeakMap(), $ = new WeakMap(), K = new WeakMap(), ne = new WeakMap(), k = new WeakMap(), y = new WeakSet(), /**
 * Routes every process spawn requested by the PHP runtime.
 *
 * Commands registered via setCommandSpawnHandler() are dispatched to
 * their dedicated handlers; setSpawnHandler() cannot displace them.
 * Every other command is delegated to the handler installed via
 * setSpawnHandler().
 */
ye = function(t, e = [], r = {}) {
  const s = Array.isArray(t) ? t : e.length ? [t, ...e] : splitShellCommand(t), n = s[0] && u(this, ne).get(basename(s[0]));
  if (n)
    return n(
      s[0],
      s.slice(1),
      r
    );
  if (u(this, K))
    return u(this, K).call(this, t, e, r);
  const o = new Error(
    `popen(), proc_open() are unsupported on this PHP instance. Call php.setSpawnHandler()
			and provide a callback to handle spawning processes, or disable popen(), proc_open() via php.ini.`
  );
  throw o.code = "SPAWN_UNSUPPORTED", o;
}, /**
 * Prepares the $_SERVER entries for the PHP runtime.
 *
 * @param defaults Default entries to include in $_SERVER.
 * @param headers HTTP headers to include in $_SERVER (as HTTP_ prefixed entries).
 * @param port HTTP port, used to determine infer $_SERVER['HTTPS'] value if none
 *             was provided.
 * @returns Computed $_SERVER entries.
 */
Ee = function(t, e, r) {
  const s = {
    ...t || {}
  };
  s.HTTPS = s.HTTPS || r === 443 ? "on" : "off";
  for (const n in e) {
    let o = "HTTP_";
    ["content-type", "content-length"].includes(n.toLowerCase()) && (o = ""), s[`${o}${n.toUpperCase().replace(/-/g, "_")}`] = e[n];
  }
  return s;
}, xe = function(t) {
  this[__private__dont__use].ccall(
    "wasm_set_request_uri",
    null,
    [STRING],
    [t]
  );
  let e = "";
  t.includes("?") && (e = t.substring(t.indexOf("?") + 1)), this[__private__dont__use].ccall(
    "wasm_set_query_string",
    null,
    [STRING],
    [e]
  );
}, Se = function(t) {
  this[__private__dont__use].ccall(
    "wasm_set_request_host",
    null,
    [STRING],
    [t]
  );
}, ke = function(t) {
  this[__private__dont__use].ccall(
    "wasm_set_request_port",
    null,
    [NUMBER],
    [t]
  );
}, Fe = function(t, e) {
  let r;
  try {
    r = parseInt(new URL(t).port, 10);
  } catch {
  }
  return (!r || isNaN(r) || r === 80) && (r = e === "https" ? 443 : 80), r;
}, be = function(t) {
  this[__private__dont__use].ccall(
    "wasm_set_request_method",
    null,
    [STRING],
    [t]
  );
}, Re = function(t) {
  t.cookie && this[__private__dont__use].ccall(
    "wasm_set_cookies",
    null,
    [STRING],
    [t.cookie]
  ), t["content-type"] && this[__private__dont__use].ccall(
    "wasm_set_content_type",
    null,
    [STRING],
    [t["content-type"]]
  ), t["content-length"] && this[__private__dont__use].ccall(
    "wasm_set_content_length",
    null,
    [NUMBER],
    [parseInt(t["content-length"], 10)]
  );
}, He = function(t) {
  let e, r;
  typeof t == "string" ? (logger.warn(
    "Passing a string as the request body is deprecated. Please use a Uint8Array instead. See https://github.com/WordPress/wordpress-playground/issues/997 for more details"
  ), r = this[__private__dont__use].lengthBytesUTF8(t), e = r + 1) : (r = t.byteLength, e = t.byteLength);
  const s = this[__private__dont__use].malloc(e);
  if (!s)
    throw new Error("Could not allocate memory for the request body.");
  return typeof t == "string" ? this[__private__dont__use].stringToUTF8(
    t,
    s,
    e + 1
  ) : this[__private__dont__use].HEAPU8.set(t, s), this[__private__dont__use].ccall(
    "wasm_set_request_body",
    null,
    [NUMBER],
    [s]
  ), this[__private__dont__use].ccall(
    "wasm_set_content_length",
    null,
    [NUMBER],
    [r]
  ), s;
}, ge = function(t) {
  this[__private__dont__use].ccall(
    "wasm_set_path_translated",
    null,
    [STRING],
    [t]
  );
}, Te = function(t, e) {
  this[__private__dont__use].ccall(
    "wasm_add_SERVER_entry",
    null,
    [STRING, STRING],
    [t, e]
  );
}, Pe = function(t, e) {
  this[__private__dont__use].ccall(
    "wasm_add_ENV_entry",
    null,
    [STRING, STRING],
    [t, e]
  );
}, we = async function(t) {
  u(this, k).enabled && u(this, k).needsRotating && await this.rotateRuntime(), ++u(this, k).requestsMade, u(this, k).requestsMade >= u(this, k).maxRequests && (u(this, k).needsRotating = !0);
  const e = this[__private__dont__use], r = await createInvertedReadableStream();
  e.onHeaders = (p) => {
    a || s || r.controller.enqueue(p.slice());
  };
  let s = !1;
  const n = () => {
    s || (s = !0, r.controller.close());
  }, o = await createInvertedReadableStream();
  e.onStdout = (p) => {
    n(), !a && o.controller.enqueue(p.slice());
  };
  const i = await createInvertedReadableStream();
  e.onStderr = (p) => {
    a || i.controller.enqueue(p.slice());
  };
  let a = !1, c;
  const d = (async () => {
    var p;
    try {
      return await Promise.race([
        t(),
        new Promise((P, S) => {
          var N;
          c = (W) => {
            isExitCode(W.error) || S(W.error);
          }, (N = u(this, G)) == null || N.addEventListener(
            "error",
            c,
            { once: !0 }
          );
        })
      ]);
    } catch (h) {
      if (isExitCode(h))
        return h.status;
      safeStreamError$1(o.controller, h), safeStreamError$1(i.controller, h), safeStreamError$1(r.controller, h), a = !0;
      for (const P in this)
        typeof this[P] == "function" && (this[P] = () => {
          throw new Error(
            "PHP runtime has crashed – see the earlier error for details."
          );
        });
      throw this.functionsMaybeMissingFromAsyncify = getFunctionsMaybeMissingFromAsyncify(), h;
    } finally {
      a || (safeStreamClose$1(o.controller), safeStreamClose$1(i.controller), n(), a = !0), (p = u(this, G)) == null || p.removeEventListener(
        "error",
        c
      );
    }
  })().then(
    (p) => (p !== 0 && this.dispatchEvent({
      type: "request.error",
      error: new Error(
        `PHP.run() failed with exit code ${p}.`
      ),
      // Distinguish between PHP request and PHP-wasm errors
      source: "php-wasm"
    }), p),
    (p) => {
      const h = p.source ?? "php-wasm";
      throw this.dispatchEvent({
        type: "request.error",
        error: p,
        source: h
      }), p;
    }
  );
  return new StreamedPHPResponse(
    r.stream,
    o.stream,
    i.stream,
    d
  );
};
function normalizeHeaders(t) {
  const e = {};
  for (const r in t)
    e[r.toLowerCase()] = t[r];
  return e;
}
function copyMEMFSNodes(t, e, r) {
  if (getNodeType(t, r) !== "memfs" || !["memfs", "missing"].includes(getNodeType(e, r)))
    return;
  const s = t.lookupPath(r, { follow: !1 });
  if (t.isLink(s.node.mode)) {
    const o = t.readlink(r);
    e.symlink(o, r);
    return;
  }
  if (!t.isDir(s.node.mode)) {
    e.writeFile(r, t.readFile(r));
    return;
  }
  e.mkdirTree(r);
  const n = t.readdir(r).filter((o) => o !== "." && o !== "..");
  for (const o of n)
    copyMEMFSNodes(t, e, joinPaths(r, o));
}
function snapshotMountPoint(t, e) {
  try {
    const r = t.lookupPath(e, { follow: !1 });
    return t.isLink(r.node.mode) ? { kind: "symlink", target: t.readlink(e) } : t.isDir(r.node.mode) ? { kind: "directory" } : { kind: "file" };
  } catch {
    return;
  }
}
function restoreMountPointSnapshot(t, e, r) {
  if (!(!r || getNodeType(t, e) !== "missing")) {
    if (r.kind === "directory") {
      t.mkdirTree(e);
      return;
    }
    t.mkdirTree(dirname(e)), r.kind === "symlink" ? t.symlink(r.target, e) : t.writeFile(e, new Uint8Array());
  }
}
function isMissingMountSourceError(t) {
  return t.phpWasmMountSourceMissing === !0;
}
function isMissingMountTargetPathError(t) {
  return t.errno === 44;
}
async function createInvertedReadableStream(t = {}) {
  let e;
  const r = new Promise(
    (o) => {
      e = o;
    }
  ), s = new ReadableStream({
    ...t,
    start(o) {
      if (e(o), t.start)
        return t.start(o);
    }
  }), n = await r;
  return {
    stream: s,
    controller: n
  };
}
function safeStreamError$1(t, e) {
  try {
    t.error(e);
  } catch {
  }
}
function safeStreamClose$1(t) {
  try {
    t.close();
  } catch {
  }
}
const getNodeType = (t, e) => {
  try {
    return "contents" in t.lookupPath(e, { follow: !0 }).node ? "memfs" : (
      /**
      * Could be NODEFS, PROXYFS, etc.
      */
      "not-memfs"
    );
  } catch {
    return "missing";
  }
};
async function getPhpIniEntries(t, e) {
  const r = parse(await t.readFileAsText(PHP_INI_PATH));
  if (e === void 0)
    return r;
  const s = {};
  for (const n of e)
    s[n] = r[n];
  return s;
}
async function setPhpIniEntries(t, e) {
  const r = parse(await t.readFileAsText(PHP_INI_PATH));
  for (const [s, n] of Object.entries(e))
    n == null ? delete r[s] : r[s] = n;
  await t.writeFile(PHP_INI_PATH, stringify(r));
}
async function withPHPIniValues(t, e, r) {
  const s = await t.readFileAsText(PHP_INI_PATH);
  try {
    return await setPhpIniEntries(t, e), await r();
  } finally {
    await t.writeFile(PHP_INI_PATH, s);
  }
}
async function printDebugDetails(t, e) {
  e && printResponseDebugDetails(
    await PHPResponse.fromStreamedResponse(e)
  ), await prettyPrintFullStackTrace(t);
}
async function prettyPrintFullStackTrace(t) {
  let e = t, r = !0;
  for (; e; )
    r || process.stderr.write(`
Caused by:

`), process.stderr.write(e.originalErrorClassName ?? e.name), process.stderr.write(": " + e.message + `
`), process.stderr.write(
      (e.stack + "").split(`
`).slice(1).join(`
`)
    ), process.stderr.write(`
`), e.response && printResponseDebugDetails(e.response), e.phpLogs && (process.stderr.write(`

==== PHP error log ====

`), process.stderr.write(e.phpLogs)), e = e.cause, r = !1;
  process.stderr.write(`
`);
}
function describeError(t, e = /* @__PURE__ */ new WeakSet(), r = 0, s = {}) {
  return r > 10 ? "[Max error cause depth exceeded]" : t instanceof Error ? t.message ? t.message : describeErrorObject(t, e, r, {
    ...s,
    suppressGenericErrorName: !0
  }) : t && typeof t == "object" ? describeErrorObject(t, e, r, s) : String(t);
}
function describeErrorObject(t, e, r, s = {}) {
  if (e.has(t))
    return "[Circular error cause]";
  e.add(t);
  const n = [];
  if (t.name && !(s.suppressGenericErrorName && t.name === "Error") && n.push(String(t.name)), t.message && n.push(String(t.message)), t.errno !== void 0 && n.push(`errno: ${t.errno}`), t.code !== void 0 && n.push(`code: ${t.code}`), t.cause && n.push(
    `caused by: ${describeError(
      t.cause,
      e,
      r + 1,
      s
    )}`
  ), n.length > 0)
    return n.join(" — ");
  if (typeof t.stack == "string")
    return t.stack;
  try {
    return JSON.stringify(t);
  } catch {
    return String(t);
  }
}
function printResponseDebugDetails(t) {
  process.stderr.write(
    `
    exitCode=${t.exitCode} httpStatusCode=${t.httpStatusCode} `
  );
  const e = t.headers && Object.keys(t.headers).length > 0;
  e || process.stderr.write("responseHeaders=(empty) "), t.text || process.stderr.write("stdout=(empty) "), t.errors || process.stderr.write("stderr=(empty) "), process.stderr.write(`
`), e && process.stderr.write(
    `
==== PHP response headers ====

${JSON.stringify(
      t.headers,
      null,
      2
    )}

`
  ), t.text && (process.stderr.write(`
==== PHP stdout ====

`), process.stderr.write(t.text)), t.errors && (process.stderr.write(`
==== PHP stderr ====

`), process.stderr.write(t.errors)), process.stderr.write(`
`);
}
class HttpCookieStore {
  constructor() {
    this.cookies = {};
  }
  rememberCookiesFromResponseHeaders(e) {
    if (e != null && e["set-cookie"])
      for (const r of e["set-cookie"])
        try {
          if (!r.includes("="))
            continue;
          const s = r.indexOf("="), n = r.substring(0, s), o = r.substring(s + 1).split(";")[0];
          this.cookies[n] = o;
        } catch (s) {
          logger.error(s);
        }
  }
  getCookieRequestHeader() {
    const e = [];
    for (const r in this.cookies)
      e.push(`${r}=${this.cookies[r]}`);
    return e.join("; ");
  }
}
function streamReadFileFromPHP(t, e) {
  return new ReadableStream({
    async pull(r) {
      const s = await t.readFileAsBuffer(e);
      r.enqueue(s), r.close();
    }
  });
}
async function* iteratePhpFiles(t, e, {
  relativePaths: r = !0,
  pathPrefix: s,
  exceptPaths: n = []
} = {}) {
  e = normalizePath(e);
  const o = [e];
  for (; o.length; ) {
    const i = o.pop();
    if (!i)
      return;
    const a = await t.listFiles(i);
    for (const c of a) {
      const l = `${i}/${c}`;
      if (n.includes(l.substring(e.length + 1)))
        continue;
      await t.isDir(l) ? o.push(l) : yield new StreamedFile(
        streamReadFileFromPHP(t, l),
        r ? joinPaths(
          s || "",
          l.substring(e.length + 1)
        ) : l
      );
    }
  }
}
function writeFilesStreamToPhp(t, e) {
  return new WritableStream({
    async write(r) {
      const s = joinPaths(e, r.name);
      r.type === "directory" ? await t.mkdir(s) : (await t.mkdir(dirname(s)), await t.writeFile(
        s,
        new Uint8Array(await r.arrayBuffer())
      ));
    }
  });
}
class SinglePHPInstanceManager {
  constructor(e) {
    if (this.isAcquired = !1, !e.php && !e.phpFactory)
      throw new Error(
        "SinglePHPInstanceManager requires either php or phpFactory"
      );
    this.php = e.php, this.phpFactory = e.phpFactory;
  }
  async getPrimaryPhp() {
    return this.php ? this.php : (this.phpPromise || (this.phpPromise = this.phpFactory().then((e) => (this.php = e, this.phpPromise = void 0, e))), this.phpPromise);
  }
  async acquirePHPInstance() {
    if (this.isAcquired)
      throw new Error(
        "The PHP instance already acquired. SinglePHPInstanceManager cannot spawn another PHP instance since, by definition, it only manages a single PHP instance."
      );
    const e = await this.getPrimaryPhp();
    return this.isAcquired = !0, {
      php: e,
      reap: () => {
        this.isAcquired = !1;
      }
    };
  }
  async [Symbol.asyncDispose]() {
    this.php && this.php.exit();
  }
}
class MaxPhpInstancesError extends Error {
  constructor(e) {
    super(
      `Requested more concurrent PHP instances than the limit (${e}).`
    ), this.name = this.constructor.name;
  }
}
class PHPProcessManager {
  constructor(e) {
    this.instances = [], this.idleInstances = [], this.maxPhpInstances = (e == null ? void 0 : e.maxPhpInstances) ?? 2, this.phpFactory = e == null ? void 0 : e.phpFactory, this.semaphore = new Semaphore({
      concurrency: this.maxPhpInstances,
      timeout: (e == null ? void 0 : e.timeout) || 3e4
    });
  }
  /**
   * Get the primary PHP instance (the first one spawned).
   * If no instance exists yet, one will be spawned and marked as idle.
   */
  async getPrimaryPhp() {
    if (this.instances.length > 0)
      return this.instances[0];
    this.primaryPhpPromise || (this.primaryPhpPromise = this.spawnInstance(!0));
    try {
      return await this.primaryPhpPromise;
    } finally {
      this.primaryPhpPromise = void 0;
    }
  }
  /**
   * Acquire a PHP instance for processing a request.
   *
   * Returns an idle instance from the pool, or spawns a new one if
   * the pool isn't at capacity. If all instances are busy, waits
   * until one becomes available.
   *
   * @throws {MaxPhpInstancesError} when the timeout is reached waiting
   *                                for an available instance.
   */
  async acquirePHPInstance() {
    let e;
    try {
      e = await this.semaphore.acquire();
    } catch (s) {
      throw s instanceof AcquireTimeoutError ? new MaxPhpInstancesError(this.maxPhpInstances) : s;
    }
    const r = await this.getOrSpawnInstance();
    return {
      php: r,
      reap: () => {
        this.idleInstances.push(r), e();
      }
    };
  }
  /**
   * Get an idle instance or spawn a new one.
   */
  async getOrSpawnInstance() {
    return this.instances.length === 0 && await this.getPrimaryPhp(), this.idleInstances.length === 0 && await this.spawnInstance(!1), this.idleInstances.pop();
  }
  /**
   * Spawn a new PHP instance.
   */
  async spawnInstance(e) {
    if (!this.phpFactory)
      throw new Error(
        "phpFactory must be set before spawning instances."
      );
    const r = await this.phpFactory({ isPrimary: e });
    return this.instances.push(r), this.idleInstances.push(r), r;
  }
  async [Symbol.asyncDispose]() {
    for (const e of this.instances)
      e.exit();
    this.instances = [], this.idleInstances = [];
  }
}
const PHPNextVersion = "next", SupportedPHPVersions = [
  "8.5",
  "8.4",
  "8.3",
  "8.2",
  "8.1",
  "8.0",
  "7.4"
], LatestSupportedPHPVersion = SupportedPHPVersions[0], SupportedPHPVersionsList = SupportedPHPVersions, LegacyPHPVersions = ["5.2"];
function isPHPNextVersion(t) {
  return t === PHPNextVersion;
}
function isLegacyPHPVersion(t) {
  return LegacyPHPVersions.includes(t ?? "");
}
const AllPHPVersions = [
  PHPNextVersion,
  ...SupportedPHPVersions,
  ...LegacyPHPVersions
], LEGACY_PHP_INI_PATH = "/internal/shared/php.ini", LEGACY_PHP_INI_CONTENT = [
  "auto_prepend_file=/internal/shared/auto_prepend_file.php",
  "memory_limit=256M",
  "ignore_repeated_errors = 1",
  "error_reporting = E_ALL",
  "display_errors = 1",
  "html_errors = 1",
  "display_startup_errors = On",
  "log_errors = 1",
  "always_populate_raw_post_data = -1",
  "upload_max_filesize = 2000M",
  "post_max_size = 2000M",
  "allow_url_fopen = On",
  "allow_url_include = Off",
  "session.save_path = /home/web_user",
  "implicit_flush = 1",
  "output_buffering = 0",
  "max_execution_time = 0",
  "max_input_time = -1",
  "disable_functions = ini_get_all",
  "opcache.enable = 0",
  "opcache.enable_cli = 0"
].join(`
`);
function createLegacyPhpIniPreRunStep() {
  return (t) => {
    t.FS.mkdirTree("/internal/shared"), t.FS.writeFile(LEGACY_PHP_INI_PATH, LEGACY_PHP_INI_CONTENT);
  };
}
const DEFAULT_BASE_URL = "http://example.com";
function toRelativeUrl(t) {
  return t.origin === "null" ? t.toString() : t.toString().substring(t.origin.length);
}
function removePathPrefix(t, e) {
  return !e || !t.startsWith(e) ? t : t.substring(e.length);
}
function ensurePathPrefix(t, e) {
  return !e || t.startsWith(e) ? t : e + t;
}
async function encodeAsMultipart(t) {
  const e = `----${Math.random().toString(36).slice(2)}`, r = `multipart/form-data; boundary=${e}`, s = new TextEncoder(), n = [];
  for (const [c, l] of Object.entries(t))
    n.push(`--${e}\r
`), n.push(`Content-Disposition: form-data; name="${c}"`), l instanceof File && n.push(`; filename="${l.name}"`), n.push(`\r
`), l instanceof File && (n.push("Content-Type: application/octet-stream"), n.push(`\r
`)), n.push(`\r
`), l instanceof File ? n.push(await fileToUint8Array(l)) : n.push(l), n.push(`\r
`);
  n.push(`--${e}--\r
`);
  const o = n.reduce((c, l) => c + l.length, 0), i = new Uint8Array(o);
  let a = 0;
  for (const c of n)
    i.set(
      typeof c == "string" ? s.encode(c) : c,
      a
    ), a += c.length;
  return { bytes: i, contentType: r };
}
function fileToUint8Array(t) {
  return t.arrayBuffer().then((e) => new Uint8Array(e));
}
const _default = "application/octet-stream", asx = "video/x-ms-asf", atom = "application/atom+xml", avi = "video/x-msvideo", avif = "image/avif", bin = "application/octet-stream", bmp = "image/x-ms-bmp", cco = "application/x-cocoa", cjs = "application/javascript", css = "text/css", data = "application/octet-stream", deb = "application/octet-stream", der = "application/x-x509-ca-cert", dmg = "application/octet-stream", doc = "application/msword", docx = "application/vnd.openxmlformats-officedocument.wordprocessingml.document", eot = "application/vnd.ms-fontobject", flv = "video/x-flv", gif = "image/gif", gz = "application/gzip", hqx = "application/mac-binhex40", htc = "text/x-component", html = "text/html", ico = "image/x-icon", iso = "application/octet-stream", jad = "text/vnd.sun.j2me.app-descriptor", jar = "application/java-archive", jardiff = "application/x-java-archive-diff", jng = "image/x-jng", jnlp = "application/x-java-jnlp-file", jpg = "image/jpeg", jpeg = "image/jpeg", js = "application/javascript", json = "application/json", kml = "application/vnd.google-earth.kml+xml", kmz = "application/vnd.google-earth.kmz", m3u8 = "application/vnd.apple.mpegurl", m4a = "audio/x-m4a", m4v = "video/x-m4v", md = "text/plain", mid = "audio/midi", mjs = "application/javascript", mml = "text/mathml", mng = "video/x-mng", mov = "video/quicktime", mp3 = "audio/mpeg", mp4 = "video/mp4", mpeg = "video/mpeg", msi = "application/octet-stream", odg = "application/vnd.oasis.opendocument.graphics", odp = "application/vnd.oasis.opendocument.presentation", ods = "application/vnd.oasis.opendocument.spreadsheet", odt = "application/vnd.oasis.opendocument.text", ogg = "audio/ogg", otf = "font/otf", pdf = "application/pdf", pl = "application/x-perl", png = "image/png", ppt = "application/vnd.ms-powerpoint", pptx = "application/vnd.openxmlformats-officedocument.presentationml.presentation", prc = "application/x-pilot", ps = "application/postscript", ra = "audio/x-realaudio", rar = "application/x-rar-compressed", rpm = "application/x-redhat-package-manager", rss = "application/rss+xml", rtf = "application/rtf", run = "application/x-makeself", sea = "application/x-sea", sit = "application/x-stuffit", svg = "image/svg+xml", swf = "application/x-shockwave-flash", tcl = "application/x-tcl", tar = "application/x-tar", tif = "image/tiff", ts = "video/mp2t", ttf = "font/ttf", txt = "text/plain", wasm = "application/wasm", wbmp = "image/vnd.wap.wbmp", webm = "video/webm", webp = "image/webp", wml = "text/vnd.wap.wml", wmlc = "application/vnd.wap.wmlc", wmv = "video/x-ms-wmv", woff = "font/woff", woff2 = "font/woff2", xhtml = "application/xhtml+xml", xls = "application/vnd.ms-excel", xlsx = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", xml = "text/xml", xpi = "application/x-xpinstall", xspf = "application/xspf+xml", zip = "application/zip", mimeTypes = {
  _default,
  "3gpp": "video/3gpp",
  "7z": "application/x-7z-compressed",
  asx,
  atom,
  avi,
  avif,
  bin,
  bmp,
  cco,
  cjs,
  css,
  data,
  deb,
  der,
  dmg,
  doc,
  docx,
  eot,
  flv,
  gif,
  gz,
  hqx,
  htc,
  html,
  ico,
  iso,
  jad,
  jar,
  jardiff,
  jng,
  jnlp,
  jpg,
  jpeg,
  js,
  json,
  kml,
  kmz,
  m3u8,
  m4a,
  m4v,
  md,
  mid,
  mjs,
  mml,
  mng,
  mov,
  mp3,
  mp4,
  mpeg,
  msi,
  odg,
  odp,
  ods,
  odt,
  ogg,
  otf,
  pdf,
  pl,
  png,
  ppt,
  pptx,
  prc,
  ps,
  ra,
  rar,
  rpm,
  rss,
  rtf,
  run,
  sea,
  sit,
  svg,
  swf,
  tcl,
  tar,
  tif,
  ts,
  ttf,
  txt,
  wasm,
  wbmp,
  webm,
  webp,
  wml,
  wmlc,
  wmv,
  woff,
  woff2,
  xhtml,
  xls,
  xlsx,
  xml,
  xpi,
  xspf,
  zip
};
var C, Z, ie, X, ee, I, te, V, oe, R, Me, le, Le, Ie, Ae;
class PHPRequestHandler {
  /**
   * The request handler needs to decide whether to serve a static asset or
   * run the PHP interpreter. For static assets it should just reuse the primary
   * PHP even if there's 50 concurrent requests to serve. However, for
   * dynamic PHP requests, it needs to grab an available interpreter.
   * Therefore, it cannot just accept PHP as an argument as serving requests
   * requires access to ProcessManager.
   *
   * @param  php    - The PHP instance.
   * @param  config - Request Handler configuration.
   */
  constructor(e) {
    _(this, R);
    _(this, C);
    _(this, Z);
    _(this, ie);
    _(this, X);
    _(this, ee);
    _(this, I);
    _(this, te);
    _(this, V);
    _(this, oe);
    const {
      documentRoot: r = "/www/",
      absoluteUrl: s = typeof location == "object" ? location.href : DEFAULT_BASE_URL,
      rewriteRules: n = [],
      pathAliases: o = [],
      getFileNotFoundAction: i = () => ({ type: "404" })
    } = e, a = (d) => {
      d.isDir(r) || d.mkdir(r), d.chdir(r), d.requestHandler = this;
    };
    if (e.php)
      a(e.php), this.instanceManager = new SinglePHPInstanceManager({
        php: e.php
      });
    else if (e.phpFactory)
      this.instanceManager = new PHPProcessManager({
        phpFactory: async (d) => {
          const p = await e.phpFactory({
            ...d,
            requestHandler: this
          });
          return a(p), p;
        },
        maxPhpInstances: e.maxPhpInstances
      });
    else
      throw new Error(
        "Either php or phpFactory must be provided in the configuration."
      );
    x(this, V, e.cookieStore === void 0 ? new HttpCookieStore() : e.cookieStore), x(this, C, r);
    const c = new URL(s);
    x(this, ie, c.hostname), x(this, X, c.port ? Number(c.port) : c.protocol === "https:" ? 443 : 80), x(this, Z, (c.protocol || "").replace(":", ""));
    const l = u(this, X) !== 443 && u(this, X) !== 80;
    x(this, ee, [
      u(this, ie),
      l ? `:${u(this, X)}` : ""
    ].join("")), x(this, I, c.pathname.replace(/\/+$/, "")), x(this, te, [
      `${u(this, Z)}://`,
      u(this, ee),
      u(this, I)
    ].join("")), this.rewriteRules = n, x(this, oe, o), this.getFileNotFoundAction = i;
  }
  async getPrimaryPhp() {
    return await this.instanceManager.getPrimaryPhp();
  }
  /**
   * Converts a path to an absolute URL based at the PHPRequestHandler
   * root.
   *
   * @param  path The server path to convert to an absolute URL.
   * @returns The absolute URL.
   */
  pathToInternalUrl(e) {
    return e.startsWith("/") || (e = `/${e}`), `${this.absoluteUrl}${e}`;
  }
  /**
   * Converts an absolute URL based at the PHPRequestHandler to a relative path
   * without the server pathname and scope.
   *
   * @param  internalUrl An absolute URL based at the PHPRequestHandler root.
   * @returns The relative path.
   */
  internalUrlToPath(e) {
    const r = new URL(e, "https://playground.internal");
    return r.pathname.startsWith(u(this, I)) && (r.pathname = r.pathname.slice(u(this, I).length)), toRelativeUrl(r);
  }
  /**
   * The absolute URL of this PHPRequestHandler instance.
   */
  get absoluteUrl() {
    return u(this, te);
  }
  /**
   * The directory in the PHP filesystem where the server will look
   * for the files to serve. Default: `/var/www`.
   */
  get documentRoot() {
    return u(this, C);
  }
  /**
   * Serves the request – either by serving a static file, or by
   * dispatching it to the PHP runtime.
   *
   * The request() method mode behaves like a web server and only works if
   * the PHP was initialized with a `requestHandler` option (which the online
   * version of WordPress Playground does by default).
   *
   * In the request mode, you pass an object containing the request information
   * (method, headers, body, etc.) and the URL to request:
   *
   * ```ts
   * await client.writeFile(
   * 	'/index.php',
   * 	`<?php echo file_get_contents('php://input');`
   * );
   * const result = await client.request({
   * 	url: '/index.php',
   * 	method: 'POST',
   * 	headers: {
   * 		'Content-Type': 'text/plain',
   * 	},
   * 	body: 'Hello world!',
   * });
   * // result.text === 'Hello world!'
   * ```
   *
   * The `request()` method cannot be used in conjunction with `cli()`.
   *
   * @example
   * ```js
   * const output = await php.request({
   * 	method: 'GET',
   * 	url: '/index.php',
   * 	headers: {
   * 		'X-foo': 'bar',
   * 	},
   * 	body: {
   * 		foo: 'bar',
   * 	},
   * });
   * console.log(output.stdout); // "Hello world!"
   * ```
   *
   * @param  request - PHP Request data.
   */
  async request(e) {
    const r = await this.requestStreamed(e), s = await PHPResponse.fromStreamedResponse(r);
    return s.ok() && s.exitCode !== 0 ? new PHPResponse(
      500,
      s.headers,
      s.bytes,
      s.errors,
      s.exitCode
    ) : s;
  }
  /**
   * Serves the request with streaming support – returns a StreamedPHPResponse
   * that allows processing the response body incrementally without buffering
   * the entire response in memory.
   *
   * This is useful for large file downloads (>2GB) that would otherwise
   * exceed JavaScript's Uint8Array size limits.
   *
   * @param request - PHP Request data.
   * @returns A StreamedPHPResponse.
   */
  async requestStreamed(e) {
    const r = looksLikeAbsoluteUrl(e.url), s = new URL(
      // Remove the hash part of the URL as it's not meant for the server.
      e.url.split("#")[0],
      r ? void 0 : DEFAULT_BASE_URL
    ), n = v(this, R, Me).call(this, s), o = await this.getPrimaryPhp(), i = removePathPrefix(
      /**
       * URL.pathname returns a URL-encoded path. We need to decode it
       * before using it as a filesystem path.
       */
      decodeURIComponent(n.pathname),
      u(this, I)
    );
    let a = v(this, R, le).call(this, i);
    if (o.isDir(a)) {
      if (!i.endsWith("/"))
        return StreamedPHPResponse.fromPHPResponse(
          new PHPResponse(
            301,
            { location: [`${n.pathname}/`] },
            new Uint8Array(0)
          )
        );
      for (const c of ["index.php", "index.html"]) {
        const l = joinPaths(a, c);
        if (o.isFile(l)) {
          a = l, n.pathname = joinPaths(
            n.pathname,
            c
          );
          break;
        }
      }
    }
    if (!o.isFile(a)) {
      let c = i;
      for (; c.startsWith("/") && c !== dirname(c); ) {
        c = dirname(c);
        const l = v(this, R, le).call(this, c);
        if (o.isFile(l) && // Only run partial path resolution for PHP files.
        l.endsWith(".php")) {
          a = v(this, R, le).call(this, c);
          break;
        }
      }
    }
    if (!o.isFile(a)) {
      const c = this.getFileNotFoundAction(
        n.pathname
      );
      switch (c.type) {
        case "response":
          return StreamedPHPResponse.fromPHPResponse(
            c.response
          );
        case "internal-redirect":
          a = joinPaths(u(this, C), c.uri);
          break;
        case "404":
          return StreamedPHPResponse.forHttpCode(404);
        default:
          throw new Error(
            `Unsupported file-not-found action type: '${c.type}'`
          );
      }
    }
    return o.isFile(a) ? a.endsWith(".php") ? await v(this, R, Ie).call(this, e, s, n, a) : StreamedPHPResponse.fromPHPResponse(
      v(this, R, Le).call(this, o, a)
    ) : StreamedPHPResponse.forHttpCode(404);
  }
  /**
   * Computes the essential $_SERVER entries for a request.
   *
   * php_wasm.c sets some defaults, assuming it runs as a CLI script.
   * This function overrides them with the values correct in the request
   * context.
   *
   * @TODO: Consolidate the $_SERVER setting logic into a single place instead
   *        of splitting it between the C SAPI and the TypeScript code. The PHP
   *        class has a `.cli()` method that could take care of the CLI-specific
   *        $_SERVER values.
   *
   * Path and URL-related $_SERVER entries are theoretically documented
   * at https://www.php.net/manual/en/reserved.variables.server.php,
   * but that page is not very helpful in practice. Here are tables derived
   * by interacting with PHP servers:
   *
   * ## PHP Dev Server
   *
   * Setup:
   *   – `/home/adam/subdir/script.php` file contains `<?php phpinfo(); ?>`
   *   – `php -S 127.0.0.1:8041` running in `/home/adam` directory
   *   – A request is sent to `http://127.0.0.1:8041/subdir/script.php/b.php/c.php`
   *
   * Results:
   *
   * $_SERVER['REQUEST_URI']    | `/subdir/script.php/b.php/c.php`
   * $_SERVER['SCRIPT_NAME']    | `/subdir/script.php`
   * $_SERVER['SCRIPT_FILENAME']| `/home/adam/subdir/script.php`
   * $_SERVER['PATH_INFO']      | `/b.php/c.php`
   * $_SERVER['PHP_SELF']       | `/subdir/script.php/b.php/c.php`
   *
   * ## Apache – rewriting rules
   *
   * Setup:
   *   – `/var/www/html/subdir/script.php` file contains `<?php phpinfo(); ?>`
   *   – Apache is listening on port 8041
   *   – The document root is `/var/www/html`
   *   – A request is sent to `http://127.0.0.1:8041/api/v1/user/123`
   *
   * .htaccess file:
   *
   * ```apache
   * RewriteEngine On
   * RewriteRule ^api/v1/user/([0-9]+)$ /subdir/script.php?endpoint=user&id=$1 [L,QSA]
   * ```
   *
   * Results:
   *
   * ```
   * $_SERVER['REQUEST_URI']             | /api/v1/user/123
   * $_SERVER['SCRIPT_NAME']             | /subdir/script.php
   * $_SERVER['SCRIPT_FILENAME']         | /var/www/html/subdir/script.php
   * $_SERVER['PATH_INFO']               | (key not set)
   * $_SERVER['PHP_SELF']                | /subdir/script.php
   * $_SERVER['QUERY_STRING']            | endpoint=user&id=123
   * $_SERVER['REDIRECT_STATUS']         | 200
   * $_SERVER['REDIRECT_URL']            | /api/v1/user/123
   * $_SERVER['REDIRECT_QUERY_STRING']   | endpoint=user&id=123
   * === $_GET Variables ===
   * $_GET['endpoint']                   | user
   * $_GET['id']                         | 123
   * ```
   *
   * ## Apache – vanilla request
   *
   * Setup:
   *    – The same as above.
   *    – A request sent http://localhost:8041/subdir/script.php?param=value
   *
   * Results:
   *
   * ```
   * $_SERVER['REQUEST_URI']     | /subdir/script.php?param=value
   * $_SERVER['SCRIPT_NAME']     | /subdir/script.php
   * $_SERVER['SCRIPT_FILENAME'] | /var/www/html/subdir/script.php
   * $_SERVER['PATH_INFO']       | (key not set)
   * $_SERVER['PHP_SELF']        | /subdir/script.php
   * $_SERVER['REDIRECT_URL']    | (key not set)
   * $_SERVER['REDIRECT_STATUS'] | (key not set)
   * $_SERVER['QUERY_STRING']    | param=value
   * $_SERVER['REQUEST_METHOD']  | GET
   * $_SERVER['DOCUMENT_ROOT']   | /var/www/html
   *
   * === $_GET Variables ===
   * $_GET['param']              | value
   * ```
   */
  prepare_$_SERVER_superglobal(e, r, s) {
    const n = {
      REMOTE_ADDR: "127.0.0.1",
      DOCUMENT_ROOT: u(this, C),
      HTTPS: u(this, te).startsWith("https://") ? "on" : ""
    };
    return n.REQUEST_URI = e.pathname + e.search, s.startsWith(u(this, C)) && (n.SCRIPT_NAME = s.substring(
      u(this, C).length
    ), n.PHP_SELF = r.pathname, n.REQUEST_URI.startsWith(n.SCRIPT_NAME) && (n.PATH_INFO = n.REQUEST_URI.substring(
      n.SCRIPT_NAME.length
    ), n.PATH_INFO.includes("?") && (n.PATH_INFO = n.PATH_INFO.substring(
      0,
      n.PATH_INFO.indexOf("?")
    )))), n.QUERY_STRING = r.search.substring(1), n;
  }
  async [Symbol.asyncDispose]() {
    await this.instanceManager[Symbol.asyncDispose]();
  }
}
C = new WeakMap(), Z = new WeakMap(), ie = new WeakMap(), X = new WeakMap(), ee = new WeakMap(), I = new WeakMap(), te = new WeakMap(), V = new WeakMap(), oe = new WeakMap(), R = new WeakSet(), /**
 * Apply the rewrite rules to the original request URL.
 *
 * @param originalRequestUrl - The original request URL.
 * @returns The rewritten request URL.
 */
Me = function(e) {
  const r = removePathPrefix(
    decodeURIComponent(e.pathname),
    u(this, I)
  ), s = applyRewriteRules(
    r,
    this.rewriteRules
  ), n = new URL(
    joinPaths(u(this, I), s),
    e.toString()
  );
  for (const [o, i] of e.searchParams.entries())
    n.searchParams.append(o, i);
  return n;
}, /**
 * Resolves a URL path to a filesystem path, checking path aliases first.
 *
 * If the URL path matches a configured alias prefix, the alias's
 * filesystem path is used instead of the document root.
 *
 * @param urlPath - The URL path to resolve (e.g., '/phpmyadmin/index.php')
 * @returns The resolved filesystem path
 */
le = function(e) {
  for (const r of u(this, oe))
    if (e === r.urlPrefix || e.startsWith(r.urlPrefix + "/")) {
      const s = e.slice(r.urlPrefix.length);
      return joinPaths(r.fsPath, s);
    }
  return joinPaths(u(this, C), e);
}, /**
 * Serves a static file from the PHP filesystem.
 *
 * @param  fsPath - Absolute path of the static file to serve.
 * @returns The response.
 */
Le = function(e, r) {
  const s = e.readFileAsBuffer(r);
  return new PHPResponse(
    200,
    {
      "content-length": [`${s.byteLength}`],
      // @TODO: Infer the content-type from the arrayBuffer instead of the
      // file path. The code below won't return the correct mime-type if the
      // extension was tampered with.
      "content-type": [inferMimeType(r)],
      "accept-ranges": ["bytes"],
      "cache-control": ["public, max-age=0"]
    },
    s
  );
}, Ie = async function(e, r, s, n) {
  let o;
  try {
    o = await this.instanceManager.acquirePHPInstance();
  } catch (a) {
    return a instanceof MaxPhpInstancesError ? StreamedPHPResponse.forHttpCode(502) : StreamedPHPResponse.forHttpCode(500);
  }
  let i;
  try {
    i = await v(this, R, Ae).call(this, o.php, e, r, s, n);
  } catch (a) {
    throw o.reap(), a;
  }
  return i.finished.finally(() => {
    o == null || o.reap();
  }), i;
}, Ae = async function(e, r, s, n, o) {
  let i = "GET";
  const a = {
    host: u(this, ee),
    ...normalizeHeaders(r.headers || {})
  };
  u(this, V) && (a.cookie = u(this, V).getCookieRequestHeader());
  let c = r.body;
  if (typeof c == "object" && !(c instanceof Uint8Array)) {
    i = "POST";
    const { bytes: d, contentType: p } = await encodeAsMultipart(c);
    c = d, a["content-type"] = p;
  }
  const l = await e.runStream({
    relativeUri: ensurePathPrefix(
      toRelativeUrl(new URL(n.toString())),
      u(this, I)
    ),
    protocol: u(this, Z),
    method: r.method || i,
    $_SERVER: this.prepare_$_SERVER_superglobal(
      s,
      n,
      o
    ),
    body: c,
    scriptPath: o,
    headers: a
  });
  if (u(this, V)) {
    const d = await l.headers;
    u(this, V).rememberCookiesFromResponseHeaders(
      d
    );
  }
  return l;
};
function inferMimeType(t) {
  const e = t.split(".").pop();
  return mimeTypes[e] || mimeTypes._default;
}
function applyRewriteRules(t, e) {
  for (const r of e)
    if (new RegExp(r.match).test(t)) {
      t = t.replace(r.match, r.replacement);
      break;
    }
  return t;
}
function looksLikeAbsoluteUrl(t) {
  try {
    return new URL(t), !0;
  } catch {
    return !1;
  }
}
function rotatePHPRuntime({
  php: t,
  recreateRuntime: e,
  maxRequests: r = 400
}) {
  return t.enableRuntimeRotation({
    recreateRuntime: e,
    maxRequests: r
  });
}
async function writeFiles(t, e, r, { rmRoot: s = !1 } = {}) {
  const n = resolveFileTree(e, r);
  s && await t.isDir(e) && await t.rmdir(e, { recursive: !0 });
  for (const [o, i] of n)
    await t.fileExists(dirname(o)) || await t.mkdir(dirname(o)), await t.writeFile(o, i);
}
function resolveFileTree(t, e) {
  return Object.entries(e).flatMap(([r, s]) => {
    const n = resolvePathUnder(r, t);
    if (!n)
      throw new Error(
        `Invalid file tree path ${JSON.stringify(
          r
        )}: it must resolve inside ${JSON.stringify(t)}.`
      );
    return s instanceof Uint8Array || typeof s == "string" ? [[n, s]] : resolveFileTree(n, s);
  });
}
const schema12 = {
  properties: {
    name: { type: "string" },
    version: { type: "string" },
    mode: { type: "string", const: "php-extension" },
    loadWithIniDirective: {
      $ref: "#/definitions/PHPExtensionLoadDirective",
      description: "The first directive of the generated startup `.ini` file. Defaults to `extension`; use `zend_extension` for Zend extensions like Xdebug. Use `false` to stage the `.so` without registering it in php.ini."
    },
    iniEntries: {
      type: "object",
      additionalProperties: { type: "string" },
      description: "Additional `key=value` lines for the generated startup `.ini` file."
    },
    env: {
      type: "object",
      additionalProperties: { type: "string" },
      description: "Environment variables added before the extension is loaded."
    },
    extensionDir: {
      type: "string",
      description: "VFS directory where PHP.wasm writes the extension `.so` file and its per-extension ini file. Defaults to `PHP_EXTENSIONS_DIR`."
    },
    artifacts: {
      type: "array",
      items: {
        type: "object",
        properties: {
          phpVersion: {
            type: "string",
            description: "PHP major/minor version, e.g. `8.4`."
          },
          sourcePath: {
            type: "string",
            description: "Relative to the manifest URL/base URL, or an absolute URL."
          },
          extraFiles: {
            $ref: "#/definitions/PHPExtensionManifestExtraFiles",
            description: "URL-backed files needed only by this artifact."
          }
        },
        required: ["phpVersion", "sourcePath"],
        additionalProperties: !1
      }
    },
    extraFiles: {
      $ref: "#/definitions/PHPExtensionManifestExtraFiles",
      description: "URL-backed files shared by every artifact in this manifest."
    }
  }
}, schema15 = {
  properties: {
    nodes: {
      items: {
        properties: {
          type: {
            enum: ["file", "directory"]
          }
        }
      }
    }
  }
}, func2 = Object.prototype.hasOwnProperty, schema14 = {
  enum: ["extension", "zend_extension"]
};
function validate12(t, { instancePath: e = "", parentData: r, parentDataProperty: s, rootData: n = t } = {}) {
  let o = null, i = 0;
  const a = i;
  let c = !1;
  const l = i;
  if (typeof t != "string") {
    const p = {
      instancePath: e,
      schemaPath: "#/definitions/PHPExtensionIniDirective/type",
      keyword: "type",
      params: { type: "string" },
      message: "must be string"
    };
    o === null ? o = [p] : o.push(p), i++;
  }
  if (!(t === "extension" || t === "zend_extension")) {
    const p = {
      instancePath: e,
      schemaPath: "#/definitions/PHPExtensionIniDirective/enum",
      keyword: "enum",
      params: { allowedValues: schema14.enum },
      message: "must be equal to one of the allowed values"
    };
    o === null ? o = [p] : o.push(p), i++;
  }
  var d = l === i;
  if (c = c || d, !c) {
    const p = i;
    if (typeof t != "boolean") {
      const P = {
        instancePath: e,
        schemaPath: "#/anyOf/1/type",
        keyword: "type",
        params: { type: "boolean" },
        message: "must be boolean"
      };
      o === null ? o = [P] : o.push(P), i++;
    }
    if (t !== !1) {
      const P = {
        instancePath: e,
        schemaPath: "#/anyOf/1/const",
        keyword: "const",
        params: { allowedValue: !1 },
        message: "must be equal to constant"
      };
      o === null ? o = [P] : o.push(P), i++;
    }
    var d = p === i;
    c = c || d;
  }
  if (c)
    i = a, o !== null && (a ? o.length = a : o = null);
  else {
    const p = {
      instancePath: e,
      schemaPath: "#/anyOf",
      keyword: "anyOf",
      params: {},
      message: "must match a schema in anyOf"
    };
    return o === null ? o = [p] : o.push(p), i++, validate12.errors = o, !1;
  }
  return validate12.errors = o, i === 0;
}
function validate11(t, { instancePath: e = "", parentData: r, parentDataProperty: s, rootData: n = t } = {}) {
  let o = null, i = 0;
  if (i === 0)
    if (t && typeof t == "object" && !Array.isArray(t)) {
      let M;
      if (t.name === void 0 && (M = "name") || t.artifacts === void 0 && (M = "artifacts"))
        return validate11.errors = [
          {
            instancePath: e,
            schemaPath: "#/required",
            keyword: "required",
            params: { missingProperty: M },
            message: "must have required property '" + M + "'"
          }
        ], !1;
      {
        const O = i;
        for (const f in t)
          if (!func2.call(schema12.properties, f))
            return validate11.errors = [
              {
                instancePath: e,
                schemaPath: "#/additionalProperties",
                keyword: "additionalProperties",
                params: { additionalProperty: f },
                message: "must NOT have additional properties"
              }
            ], !1;
        if (O === i) {
          if (t.name !== void 0) {
            const f = i;
            if (typeof t.name != "string")
              return validate11.errors = [
                {
                  instancePath: e + "/name",
                  schemaPath: "#/properties/name/type",
                  keyword: "type",
                  params: { type: "string" },
                  message: "must be string"
                }
              ], !1;
            var a = f === i;
          } else
            var a = !0;
          if (a) {
            if (t.version !== void 0) {
              const f = i;
              if (typeof t.version != "string")
                return validate11.errors = [
                  {
                    instancePath: e + "/version",
                    schemaPath: "#/properties/version/type",
                    keyword: "type",
                    params: { type: "string" },
                    message: "must be string"
                  }
                ], !1;
              var a = f === i;
            } else
              var a = !0;
            if (a) {
              if (t.mode !== void 0) {
                let f = t.mode;
                const b = i;
                if (typeof f != "string")
                  return validate11.errors = [
                    {
                      instancePath: e + "/mode",
                      schemaPath: "#/properties/mode/type",
                      keyword: "type",
                      params: { type: "string" },
                      message: "must be string"
                    }
                  ], !1;
                if (f !== "php-extension")
                  return validate11.errors = [
                    {
                      instancePath: e + "/mode",
                      schemaPath: "#/properties/mode/const",
                      keyword: "const",
                      params: {
                        allowedValue: "php-extension"
                      },
                      message: "must be equal to constant"
                    }
                  ], !1;
                var a = b === i;
              } else
                var a = !0;
              if (a) {
                if (t.loadWithIniDirective !== void 0) {
                  const f = i;
                  validate12(t.loadWithIniDirective, {
                    instancePath: e + "/loadWithIniDirective",
                    parentData: t,
                    parentDataProperty: "loadWithIniDirective",
                    rootData: n
                  }) || (o = o === null ? validate12.errors : o.concat(
                    validate12.errors
                  ), i = o.length);
                  var a = f === i;
                } else
                  var a = !0;
                if (a) {
                  if (t.iniEntries !== void 0) {
                    let f = t.iniEntries;
                    const b = i;
                    if (i === b)
                      if (f && typeof f == "object" && !Array.isArray(f))
                        for (const U in f) {
                          const m = i;
                          if (typeof f[U] != "string")
                            return validate11.errors = [
                              {
                                instancePath: e + "/iniEntries/" + U.replace(
                                  /~/g,
                                  "~0"
                                ).replace(
                                  /\//g,
                                  "~1"
                                ),
                                schemaPath: "#/properties/iniEntries/additionalProperties/type",
                                keyword: "type",
                                params: {
                                  type: "string"
                                },
                                message: "must be string"
                              }
                            ], !1;
                          var c = m === i;
                          if (!c)
                            break;
                        }
                      else
                        return validate11.errors = [
                          {
                            instancePath: e + "/iniEntries",
                            schemaPath: "#/properties/iniEntries/type",
                            keyword: "type",
                            params: {
                              type: "object"
                            },
                            message: "must be object"
                          }
                        ], !1;
                    var a = b === i;
                  } else
                    var a = !0;
                  if (a) {
                    if (t.env !== void 0) {
                      let f = t.env;
                      const b = i;
                      if (i === b)
                        if (f && typeof f == "object" && !Array.isArray(f))
                          for (const U in f) {
                            const m = i;
                            if (typeof f[U] != "string")
                              return validate11.errors = [
                                {
                                  instancePath: e + "/env/" + U.replace(
                                    /~/g,
                                    "~0"
                                  ).replace(
                                    /\//g,
                                    "~1"
                                  ),
                                  schemaPath: "#/properties/env/additionalProperties/type",
                                  keyword: "type",
                                  params: {
                                    type: "string"
                                  },
                                  message: "must be string"
                                }
                              ], !1;
                            var l = m === i;
                            if (!l)
                              break;
                          }
                        else
                          return validate11.errors = [
                            {
                              instancePath: e + "/env",
                              schemaPath: "#/properties/env/type",
                              keyword: "type",
                              params: {
                                type: "object"
                              },
                              message: "must be object"
                            }
                          ], !1;
                      var a = b === i;
                    } else
                      var a = !0;
                    if (a) {
                      if (t.extensionDir !== void 0) {
                        const f = i;
                        if (typeof t.extensionDir != "string")
                          return validate11.errors = [
                            {
                              instancePath: e + "/extensionDir",
                              schemaPath: "#/properties/extensionDir/type",
                              keyword: "type",
                              params: {
                                type: "string"
                              },
                              message: "must be string"
                            }
                          ], !1;
                        var a = f === i;
                      } else
                        var a = !0;
                      if (a) {
                        if (t.artifacts !== void 0) {
                          let f = t.artifacts;
                          const b = i;
                          if (i === b)
                            if (Array.isArray(f)) {
                              var d = !0;
                              const U = f.length;
                              for (let m = 0; m < U; m++) {
                                let w = f[m];
                                const B = i;
                                if (i === B)
                                  if (w && typeof w == "object" && !Array.isArray(
                                    w
                                  )) {
                                    let q;
                                    if (w.phpVersion === void 0 && (q = "phpVersion") || w.sourcePath === void 0 && (q = "sourcePath"))
                                      return validate11.errors = [
                                        {
                                          instancePath: e + "/artifacts/" + m,
                                          schemaPath: "#/properties/artifacts/items/required",
                                          keyword: "required",
                                          params: {
                                            missingProperty: q
                                          },
                                          message: "must have required property '" + q + "'"
                                        }
                                      ], !1;
                                    {
                                      const H = i;
                                      for (const g in w)
                                        if (!(g === "phpVersion" || g === "sourcePath" || g === "extraFiles"))
                                          return validate11.errors = [
                                            {
                                              instancePath: e + "/artifacts/" + m,
                                              schemaPath: "#/properties/artifacts/items/additionalProperties",
                                              keyword: "additionalProperties",
                                              params: {
                                                additionalProperty: g
                                              },
                                              message: "must NOT have additional properties"
                                            }
                                          ], !1;
                                      if (H === i) {
                                        if (w.phpVersion !== void 0) {
                                          const g = i;
                                          if (typeof w.phpVersion != "string")
                                            return validate11.errors = [
                                              {
                                                instancePath: e + "/artifacts/" + m + "/phpVersion",
                                                schemaPath: "#/properties/artifacts/items/properties/phpVersion/type",
                                                keyword: "type",
                                                params: {
                                                  type: "string"
                                                },
                                                message: "must be string"
                                              }
                                            ], !1;
                                          var p = g === i;
                                        } else
                                          var p = !0;
                                        if (p) {
                                          if (w.sourcePath !== void 0) {
                                            const g = i;
                                            if (typeof w.sourcePath != "string")
                                              return validate11.errors = [
                                                {
                                                  instancePath: e + "/artifacts/" + m + "/sourcePath",
                                                  schemaPath: "#/properties/artifacts/items/properties/sourcePath/type",
                                                  keyword: "type",
                                                  params: {
                                                    type: "string"
                                                  },
                                                  message: "must be string"
                                                }
                                              ], !1;
                                            var p = g === i;
                                          } else
                                            var p = !0;
                                          if (p)
                                            if (w.extraFiles !== void 0) {
                                              let g = w.extraFiles;
                                              const ae = i;
                                              if (i === i)
                                                if (g && typeof g == "object" && !Array.isArray(
                                                  g
                                                )) {
                                                  const fe = i;
                                                  for (const E in g)
                                                    if (!(E === "vfsRoot" || E === "nodes"))
                                                      return validate11.errors = [
                                                        {
                                                          instancePath: e + "/artifacts/" + m + "/extraFiles",
                                                          schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/additionalProperties",
                                                          keyword: "additionalProperties",
                                                          params: {
                                                            additionalProperty: E
                                                          },
                                                          message: "must NOT have additional properties"
                                                        }
                                                      ], !1;
                                                  if (fe === i) {
                                                    if (g.vfsRoot !== void 0) {
                                                      const E = i;
                                                      if (typeof g.vfsRoot != "string")
                                                        return validate11.errors = [
                                                          {
                                                            instancePath: e + "/artifacts/" + m + "/extraFiles/vfsRoot",
                                                            schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/vfsRoot/type",
                                                            keyword: "type",
                                                            params: {
                                                              type: "string"
                                                            },
                                                            message: "must be string"
                                                          }
                                                        ], !1;
                                                      var h = E === i;
                                                    } else
                                                      var h = !0;
                                                    if (h)
                                                      if (g.nodes !== void 0) {
                                                        let E = g.nodes;
                                                        const ce = i;
                                                        if (i === ce)
                                                          if (Array.isArray(
                                                            E
                                                          )) {
                                                            var P = !0;
                                                            const Ne = E.length;
                                                            for (let A = 0; A < Ne; A++) {
                                                              let L = E[A];
                                                              const _e = i;
                                                              if (i === _e)
                                                                if (L && typeof L == "object" && !Array.isArray(
                                                                  L
                                                                )) {
                                                                  let he;
                                                                  if (L.vfsPath === void 0 && (he = "vfsPath"))
                                                                    return validate11.errors = [
                                                                      {
                                                                        instancePath: e + "/artifacts/" + m + "/extraFiles/nodes/" + A,
                                                                        schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/required",
                                                                        keyword: "required",
                                                                        params: {
                                                                          missingProperty: he
                                                                        },
                                                                        message: "must have required property '" + he + "'"
                                                                      }
                                                                    ], !1;
                                                                  {
                                                                    const Oe = i;
                                                                    for (const T in L)
                                                                      if (!(T === "vfsPath" || T === "type" || T === "sourcePath"))
                                                                        return validate11.errors = [
                                                                          {
                                                                            instancePath: e + "/artifacts/" + m + "/extraFiles/nodes/" + A,
                                                                            schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/additionalProperties",
                                                                            keyword: "additionalProperties",
                                                                            params: {
                                                                              additionalProperty: T
                                                                            },
                                                                            message: "must NOT have additional properties"
                                                                          }
                                                                        ], !1;
                                                                    if (Oe === i) {
                                                                      if (L.vfsPath !== void 0) {
                                                                        const T = i;
                                                                        if (typeof L.vfsPath != "string")
                                                                          return validate11.errors = [
                                                                            {
                                                                              instancePath: e + "/artifacts/" + m + "/extraFiles/nodes/" + A + "/vfsPath",
                                                                              schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/properties/vfsPath/type",
                                                                              keyword: "type",
                                                                              params: {
                                                                                type: "string"
                                                                              },
                                                                              message: "must be string"
                                                                            }
                                                                          ], !1;
                                                                        var S = T === i;
                                                                      } else
                                                                        var S = !0;
                                                                      if (S) {
                                                                        if (L.type !== void 0) {
                                                                          let T = L.type;
                                                                          const Ue = i;
                                                                          if (typeof T != "string")
                                                                            return validate11.errors = [
                                                                              {
                                                                                instancePath: e + "/artifacts/" + m + "/extraFiles/nodes/" + A + "/type",
                                                                                schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/properties/type/type",
                                                                                keyword: "type",
                                                                                params: {
                                                                                  type: "string"
                                                                                },
                                                                                message: "must be string"
                                                                              }
                                                                            ], !1;
                                                                          if (!(T === "file" || T === "directory"))
                                                                            return validate11.errors = [
                                                                              {
                                                                                instancePath: e + "/artifacts/" + m + "/extraFiles/nodes/" + A + "/type",
                                                                                schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/properties/type/enum",
                                                                                keyword: "enum",
                                                                                params: {
                                                                                  allowedValues: schema15.properties.nodes.items.properties.type.enum
                                                                                },
                                                                                message: "must be equal to one of the allowed values"
                                                                              }
                                                                            ], !1;
                                                                          var S = Ue === i;
                                                                        } else
                                                                          var S = !0;
                                                                        if (S)
                                                                          if (L.sourcePath !== void 0) {
                                                                            const T = i;
                                                                            if (typeof L.sourcePath != "string")
                                                                              return validate11.errors = [
                                                                                {
                                                                                  instancePath: e + "/artifacts/" + m + "/extraFiles/nodes/" + A + "/sourcePath",
                                                                                  schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/properties/sourcePath/type",
                                                                                  keyword: "type",
                                                                                  params: {
                                                                                    type: "string"
                                                                                  },
                                                                                  message: "must be string"
                                                                                }
                                                                              ], !1;
                                                                            var S = T === i;
                                                                          } else
                                                                            var S = !0;
                                                                      }
                                                                    }
                                                                  }
                                                                } else
                                                                  return validate11.errors = [
                                                                    {
                                                                      instancePath: e + "/artifacts/" + m + "/extraFiles/nodes/" + A,
                                                                      schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/type",
                                                                      keyword: "type",
                                                                      params: {
                                                                        type: "object"
                                                                      },
                                                                      message: "must be object"
                                                                    }
                                                                  ], !1;
                                                              var P = _e === i;
                                                              if (!P)
                                                                break;
                                                            }
                                                          } else
                                                            return validate11.errors = [
                                                              {
                                                                instancePath: e + "/artifacts/" + m + "/extraFiles/nodes",
                                                                schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/type",
                                                                keyword: "type",
                                                                params: {
                                                                  type: "array"
                                                                },
                                                                message: "must be array"
                                                              }
                                                            ], !1;
                                                        var h = ce === i;
                                                      } else
                                                        var h = !0;
                                                  }
                                                } else
                                                  return validate11.errors = [
                                                    {
                                                      instancePath: e + "/artifacts/" + m + "/extraFiles",
                                                      schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/type",
                                                      keyword: "type",
                                                      params: {
                                                        type: "object"
                                                      },
                                                      message: "must be object"
                                                    }
                                                  ], !1;
                                              var p = ae === i;
                                            } else
                                              var p = !0;
                                        }
                                      }
                                    }
                                  } else
                                    return validate11.errors = [
                                      {
                                        instancePath: e + "/artifacts/" + m,
                                        schemaPath: "#/properties/artifacts/items/type",
                                        keyword: "type",
                                        params: {
                                          type: "object"
                                        },
                                        message: "must be object"
                                      }
                                    ], !1;
                                var d = B === i;
                                if (!d)
                                  break;
                              }
                            } else
                              return validate11.errors = [
                                {
                                  instancePath: e + "/artifacts",
                                  schemaPath: "#/properties/artifacts/type",
                                  keyword: "type",
                                  params: {
                                    type: "array"
                                  },
                                  message: "must be array"
                                }
                              ], !1;
                          var a = b === i;
                        } else
                          var a = !0;
                        if (a)
                          if (t.extraFiles !== void 0) {
                            let f = t.extraFiles;
                            const b = i;
                            if (i === i)
                              if (f && typeof f == "object" && !Array.isArray(
                                f
                              )) {
                                const m = i;
                                for (const w in f)
                                  if (!(w === "vfsRoot" || w === "nodes"))
                                    return validate11.errors = [
                                      {
                                        instancePath: e + "/extraFiles",
                                        schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/additionalProperties",
                                        keyword: "additionalProperties",
                                        params: {
                                          additionalProperty: w
                                        },
                                        message: "must NOT have additional properties"
                                      }
                                    ], !1;
                                if (m === i) {
                                  if (f.vfsRoot !== void 0) {
                                    const w = i;
                                    if (typeof f.vfsRoot != "string")
                                      return validate11.errors = [
                                        {
                                          instancePath: e + "/extraFiles/vfsRoot",
                                          schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/vfsRoot/type",
                                          keyword: "type",
                                          params: {
                                            type: "string"
                                          },
                                          message: "must be string"
                                        }
                                      ], !1;
                                    var N = w === i;
                                  } else
                                    var N = !0;
                                  if (N)
                                    if (f.nodes !== void 0) {
                                      let w = f.nodes;
                                      const B = i;
                                      if (i === B)
                                        if (Array.isArray(
                                          w
                                        )) {
                                          var W = !0;
                                          const q = w.length;
                                          for (let H = 0; H < q; H++) {
                                            let g = w[H];
                                            const ae = i;
                                            if (i === ae)
                                              if (g && typeof g == "object" && !Array.isArray(
                                                g
                                              )) {
                                                let pe;
                                                if (g.vfsPath === void 0 && (pe = "vfsPath"))
                                                  return validate11.errors = [
                                                    {
                                                      instancePath: e + "/extraFiles/nodes/" + H,
                                                      schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/required",
                                                      keyword: "required",
                                                      params: {
                                                        missingProperty: pe
                                                      },
                                                      message: "must have required property '" + pe + "'"
                                                    }
                                                  ], !1;
                                                {
                                                  const fe = i;
                                                  for (const E in g)
                                                    if (!(E === "vfsPath" || E === "type" || E === "sourcePath"))
                                                      return validate11.errors = [
                                                        {
                                                          instancePath: e + "/extraFiles/nodes/" + H,
                                                          schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/additionalProperties",
                                                          keyword: "additionalProperties",
                                                          params: {
                                                            additionalProperty: E
                                                          },
                                                          message: "must NOT have additional properties"
                                                        }
                                                      ], !1;
                                                  if (fe === i) {
                                                    if (g.vfsPath !== void 0) {
                                                      const E = i;
                                                      if (typeof g.vfsPath != "string")
                                                        return validate11.errors = [
                                                          {
                                                            instancePath: e + "/extraFiles/nodes/" + H + "/vfsPath",
                                                            schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/properties/vfsPath/type",
                                                            keyword: "type",
                                                            params: {
                                                              type: "string"
                                                            },
                                                            message: "must be string"
                                                          }
                                                        ], !1;
                                                      var F = E === i;
                                                    } else
                                                      var F = !0;
                                                    if (F) {
                                                      if (g.type !== void 0) {
                                                        let E = g.type;
                                                        const ce = i;
                                                        if (typeof E != "string")
                                                          return validate11.errors = [
                                                            {
                                                              instancePath: e + "/extraFiles/nodes/" + H + "/type",
                                                              schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/properties/type/type",
                                                              keyword: "type",
                                                              params: {
                                                                type: "string"
                                                              },
                                                              message: "must be string"
                                                            }
                                                          ], !1;
                                                        if (!(E === "file" || E === "directory"))
                                                          return validate11.errors = [
                                                            {
                                                              instancePath: e + "/extraFiles/nodes/" + H + "/type",
                                                              schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/properties/type/enum",
                                                              keyword: "enum",
                                                              params: {
                                                                allowedValues: schema15.properties.nodes.items.properties.type.enum
                                                              },
                                                              message: "must be equal to one of the allowed values"
                                                            }
                                                          ], !1;
                                                        var F = ce === i;
                                                      } else
                                                        var F = !0;
                                                      if (F)
                                                        if (g.sourcePath !== void 0) {
                                                          const E = i;
                                                          if (typeof g.sourcePath != "string")
                                                            return validate11.errors = [
                                                              {
                                                                instancePath: e + "/extraFiles/nodes/" + H + "/sourcePath",
                                                                schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/properties/sourcePath/type",
                                                                keyword: "type",
                                                                params: {
                                                                  type: "string"
                                                                },
                                                                message: "must be string"
                                                              }
                                                            ], !1;
                                                          var F = E === i;
                                                        } else
                                                          var F = !0;
                                                    }
                                                  }
                                                }
                                              } else
                                                return validate11.errors = [
                                                  {
                                                    instancePath: e + "/extraFiles/nodes/" + H,
                                                    schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/items/type",
                                                    keyword: "type",
                                                    params: {
                                                      type: "object"
                                                    },
                                                    message: "must be object"
                                                  }
                                                ], !1;
                                            var W = ae === i;
                                            if (!W)
                                              break;
                                          }
                                        } else
                                          return validate11.errors = [
                                            {
                                              instancePath: e + "/extraFiles/nodes",
                                              schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/properties/nodes/type",
                                              keyword: "type",
                                              params: {
                                                type: "array"
                                              },
                                              message: "must be array"
                                            }
                                          ], !1;
                                      var N = B === i;
                                    } else
                                      var N = !0;
                                }
                              } else
                                return validate11.errors = [
                                  {
                                    instancePath: e + "/extraFiles",
                                    schemaPath: "#/definitions/PHPExtensionManifestExtraFiles/type",
                                    keyword: "type",
                                    params: {
                                      type: "object"
                                    },
                                    message: "must be object"
                                  }
                                ], !1;
                            var a = b === i;
                          } else
                            var a = !0;
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    } else
      return validate11.errors = [
        {
          instancePath: e,
          schemaPath: "#/type",
          keyword: "type",
          params: { type: "object" },
          message: "must be object"
        }
      ], !1;
  return validate11.errors = o, i === 0;
}
function validate10(t, { instancePath: e = "", parentData: r, parentDataProperty: s, rootData: n = t } = {}) {
  let o = null, i = 0;
  return validate11(t, {
    instancePath: e,
    parentData: r,
    parentDataProperty: s,
    rootData: n
  }) || (o = o === null ? validate11.errors : o.concat(validate11.errors), i = o.length), validate10.errors = o, i === 0;
}
const PHP_EXTENSIONS_DIR = "/internal/shared/extensions", MAX_EXTENSION_SIDECAR_FILE_REQUESTS = 5;
async function resolvePHPExtension(t) {
  const e = t.fetch ?? globalThis.fetch, r = t.source;
  let s = t.name, n;
  const o = {}, i = [];
  let a, c, l, d;
  if (r.format === "so") {
    if (s || (s = r.name), !s)
      throw new Error(
        "name is required when loading an extension from direct bytes."
      );
    n = toUint8Array(r.bytes);
  } else if (r.format === "url") {
    let F;
    try {
      F = new URL(String(r.url));
    } catch {
      throw new Error(
        `source.url must be an absolute URL when loading a PHP extension from a direct URL. Received: ${String(
          r.url
        )}`
      );
    }
    if (s || (s = r.name), !s && F.pathname.endsWith(".so") && (s = basename(F.pathname).slice(0, -3)), !s)
      throw new Error(
        "name is required when loading an extension from a direct URL."
      );
    n = await fetchBytes(e, F);
  } else {
    let F, M;
    if ("manifest" in r ? (F = r.manifest, r.baseUrl && (M = new URL(String(r.baseUrl)))) : (M = new URL(String(r.manifestUrl)), F = await (await e(M)).json()), !validate10(F))
      throw new Error(
        `Invalid PHP extension manifest: ${JSON.stringify(
          validate10.errors
        )}`
      );
    const O = F;
    if (!M)
      throw new Error(
        "Manifest artifacts require a manifest URL or baseUrl so relative files can be resolved."
      );
    const f = O.artifacts.find(
      (m) => m.phpVersion === t.phpVersion
    );
    if (!f)
      throw new Error(
        `No extension artifact found for PHP ${t.phpVersion}.`
      );
    s ?? (s = O.name), a = O.loadWithIniDirective, c = O.iniEntries, l = O.env, d = O.extensionDir;
    const b = new Semaphore({
      concurrency: MAX_EXTENSION_SIDECAR_FILE_REQUESTS
    }), ue = [];
    for (const m of [O.extraFiles, f.extraFiles])
      for (const w of (m == null ? void 0 : m.nodes) ?? []) {
        const B = joinPaths(m.vfsRoot ?? "", w.vfsPath);
        if (w.type === "directory") {
          i.push(B);
          continue;
        }
        if (!w.sourcePath) continue;
        const Ce = new URL(w.sourcePath, M);
        ue.push(
          b.run(() => fetchBytes(e, Ce)).then((q) => {
            o[B] = q;
          })
        );
      }
    const [U] = await Promise.all([
      fetchBytes(e, new URL(f.sourcePath, M)),
      ...ue
    ]);
    n = U;
  }
  const p = normalizePath(
    t.extensionDir ?? d ?? PHP_EXTENSIONS_DIR
  );
  t.extraFiles && (Object.assign(o, t.extraFiles.files), i.push(...t.extraFiles.directories ?? []));
  const h = t.loadWithIniDirective ?? a ?? "extension", P = {
    ...c,
    ...t.iniEntries
  }, S = joinPaths(p, `${s}.so`), N = createPHPExtensionIniFile({
    directive: h,
    extensionDir: p,
    name: s,
    soPath: S,
    iniEntries: P
  }), W = {
    ...l,
    ...t.env
  };
  return {
    soPath: S,
    soBytes: n,
    ...N,
    extraFiles: {
      files: o,
      directories: i
    },
    env: Object.keys(W).length ? W : void 0,
    extensionDir: p
  };
}
function withResolvedPHPExtensions(t, e) {
  var n;
  if (!e.length)
    return t;
  const r = { ...t.ENV };
  for (const o of e) {
    if (Object.assign(r, o.env), !o.iniPath)
      continue;
    const i = ((n = r.PHP_INI_SCAN_DIR) == null ? void 0 : n.split(":")) ?? [];
    i.includes(o.extensionDir) || (i.push(o.extensionDir), r.PHP_INI_SCAN_DIR = i.join(":"));
  }
  const s = t.preRun ?? [];
  return {
    ...t,
    ENV: r,
    preRun: [
      ...s,
      (o) => {
        for (const i of e)
          installPHPExtensionFilesSync(o.FS, i);
      }
    ]
  };
}
function installPHPExtensionFilesSync(t, e) {
  let r;
  if ("soPath" in e)
    r = e;
  else {
    const s = e.extensionDir ?? PHP_EXTENSIONS_DIR, n = e.loadWithIniDirective ?? "extension", o = joinPaths(s, `${e.name}.so`), i = createPHPExtensionIniFile({
      directive: n,
      extensionDir: s,
      name: e.name,
      soPath: o,
      iniEntries: e.iniEntries
    });
    r = {
      soPath: o,
      soBytes: toUint8Array(e.soBytes),
      ...i,
      extraFiles: e.extraFiles,
      env: e.env,
      extensionDir: s
    };
  }
  if (mkdirIfMissing(t, r.extensionDir), t.writeFile(r.soPath, r.soBytes), r.iniPath && r.iniContent !== void 0 && t.writeFile(r.iniPath, r.iniContent), r.extraFiles) {
    const { directories: s = [], files: n } = r.extraFiles;
    for (const o of s)
      mkdirIfMissing(t, o);
    for (const [o, i] of Object.entries(n))
      mkdirIfMissing(t, dirname(o)), t.writeFile(o, i);
  }
  return r;
}
function createPHPExtensionIniFile(t) {
  if (t.directive === !1)
    return {};
  const e = [
    `${t.directive}=${t.soPath}`,
    ...Object.entries(t.iniEntries ?? {}).map(
      ([r, s]) => `${r}=${s}`
    )
  ];
  return {
    iniPath: joinPaths(t.extensionDir, `${t.name}.ini`),
    iniContent: e.join(`
`)
  };
}
function mkdirIfMissing(t, e) {
  FSHelpers.fileExists(t, e) || t.mkdirTree(e);
}
async function fetchBytes(t, e) {
  const r = await t(e);
  if (!r.ok)
    throw new Error(`Failed to fetch ${String(e)}: ${r.status}`);
  return new Uint8Array(await r.arrayBuffer());
}
function toUint8Array(t) {
  return t instanceof Uint8Array ? t : new Uint8Array(t);
}
function isLegacyPhpInstance(t) {
  var n;
  const e = Object.getOwnPropertySymbols(t)[0], r = t[e], s = (n = r == null ? void 0 : r.phpVersion) == null ? void 0 : n.major;
  return typeof s == "number" && s < 7;
}
function ensureProxyFSHasMmapSupport(t) {
  const e = Object.getOwnPropertySymbols(t)[0], r = t[e], s = r.PROXYFS, n = r.FS;
  s.stream_ops.mmap || (s.stream_ops.mmap = function(o, i, a, c, l) {
    if (!n.isFile(o.node.mode))
      throw new n.ErrnoError(19);
    if (a !== 0)
      throw new n.ErrnoError(22);
    const d = r.malloc(i);
    if (!d)
      throw new n.ErrnoError(48);
    const p = r.HEAPU8.subarray(d, d + i);
    let h = 0;
    for (; h < i; ) {
      const P = o.stream_ops.read(
        o,
        p,
        h,
        i - h,
        h
      );
      if (P <= 0) break;
      h += P;
    }
    if (h !== i)
      throw r.free(d), new n.ErrnoError(5);
    return { ptr: d, allocated: !0 };
  }, s.stream_ops.msync = function(o, i, a, c, l) {
    return l & 2 || o.stream_ops.write(
      o,
      i,
      a,
      c,
      a,
      !1
    ), 0;
  });
}
async function proxyFileSystem(t, e, r) {
  const s = isLegacyPhpInstance(e), n = Object.getOwnPropertySymbols(t)[0];
  for (const o of r)
    t.fileExists(o) || t.mkdir(o), e.mkdir(o), await e.mount(o, (i) => {
      s || ensureProxyFSHasMmapSupport(i);
      const a = Object.getOwnPropertySymbols(i)[0];
      return i[a].FS.mount(
        // @ts-ignore
        i[a].PROXYFS,
        {
          root: o,
          // @ts-ignore
          fs: t[n].FS
        },
        o
      ), () => {
        try {
          i[a].FS.unmount(o);
        } catch {
        }
      };
    });
  e.addEventListener("request.end", () => {
    t.dispatchEvent({ type: "proxyfs.request.end" });
  });
}
function isPathToSharedFS(t, e) {
  var o;
  const r = Object.getOwnPropertySymbols(t)[0], n = t[r].FS.lookupPath(e, { noent_okay: !0 });
  return ((o = n == null ? void 0 : n.node) == null ? void 0 : o.isSharedFS) ?? !1;
}
function sandboxedSpawnHandlerFactory(t) {
  return createSpawnHandler(async function(e, r, s) {
    r.notifySpawn(), (e == null ? void 0 : e[0]) === "/bin/sh" && (e == null ? void 0 : e[1]) === "-c" && typeof e[2] == "string" && (e = splitShellCommand(e[2])), e[0] === "exec" && e.shift(), (e[0].endsWith(".php") || e[0].endsWith(".phar")) && e.unshift("php");
    const n = e[0].split("/").pop();
    if (e[0] === "/usr/bin/env" && e[1] === "stty" && e[2] === "size")
      r.stdout("18 140"), r.exit(0);
    else if (n === "tput" && e[1] === "cols")
      r.stdout("140"), r.exit(0);
    else if (n === "less") {
      r.on("stdin", (a) => {
        r.stdout(a);
      }), await new Promise((a) => {
        r.childProcess.stdin.on("finish", () => {
          a(!0);
        });
      }), r.exit(0);
      return;
    }
    if (!["php", "ls", "pwd"].includes(n ?? "")) {
      r.exit(127);
      return;
    }
    if (!t) {
      logger.warn(
        "Tried to spawn a PHP subprocess, but the sandboxed spawn handler was created without a getPHPInstance function."
      ), r.exit(127);
      return;
    }
    const { php: o, reap: i } = await t();
    try {
      s.cwd && await o.chdir(s.cwd);
      const a = await o.cwd();
      switch (n) {
        case "php": {
          const c = await o.cli(e, {
            env: {
              ...s.env,
              SCRIPT_PATH: e[1],
              // Set SHELL_PIPE to 0 to ensure WP-CLI formats
              // the output as ASCII tables.
              // @see https://github.com/wp-cli/wp-cli/issues/1102
              SHELL_PIPE: "0"
            }
          });
          c.stdout.pipeTo(
            new WritableStream({
              write(l) {
                r.stdout(l);
              }
            })
          ), c.stderr.pipeTo(
            new WritableStream({
              write(l) {
                r.stderr(l);
              }
            })
          ), r.exit(await c.exitCode);
          break;
        }
        case "ls": {
          const c = await o.listFiles(e[1] ?? a);
          for (const l of c)
            r.stdout(l + `
`);
          await new Promise((l) => setTimeout(l, 10)), r.exit(0);
          break;
        }
        case "pwd": {
          r.stdout(a + `
`), await new Promise((c) => setTimeout(c, 10)), r.exit(0);
          break;
        }
      }
    } catch (a) {
      const c = a instanceof Error ? a.message + `
` + a.stack : typeof a == "object" && a !== null ? JSON.stringify(a, Object.getOwnPropertyNames(a)) : String(a);
      throw r.stderr(`[spawn error] ${c}`), r.exit(1), a;
    } finally {
      i();
    }
  });
}
function exposeSync(t, e, r, s = ["*"]) {
  return expose(t, e, s, r.afterResponseSent);
}
function createSyncProxy(t, e = [], r) {
  return new Proxy(() => {
  }, {
    get(s, n) {
      return n === "then" && !e.length ? {
        then: (o, i) => i(createSyncProxy(t, [], r))
      } : createSyncProxy(t, [...e, n], r);
    },
    set(s, n, o) {
      const [i, a] = toWireValue(o);
      return r.send(
        t,
        {
          type: MessageType.SET,
          path: [...e, n].map(String),
          value: i
        },
        a
      ), !0;
    },
    apply(s, n, o) {
      if (e.at(-1) === "bind")
        return createSyncProxy(t, e.slice(0, -1), r);
      const [a, c] = processArguments(o), l = r.send(
        t,
        {
          type: MessageType.APPLY,
          path: e.map(String),
          argumentList: a
        },
        c
      );
      return fromWireValue(l);
    },
    construct(s, n) {
      const [o, i] = processArguments(n), a = r.send(
        t,
        {
          type: MessageType.CONSTRUCT,
          path: e.map(String),
          argumentList: o
        },
        i
      );
      return fromWireValue(a);
    }
  });
}
function wrapSync(t, e) {
  return createSyncProxy(t, [], e);
}
class NodeSABSyncReceiveMessageTransport {
  static async create() {
    if (!NodeSABSyncReceiveMessageTransport.receiveMessageOnPort)
      try {
        NodeSABSyncReceiveMessageTransport.receiveMessageOnPort = require("worker_threads").receiveMessageOnPort;
      } catch {
        NodeSABSyncReceiveMessageTransport.receiveMessageOnPort = await import("worker_threads").then(
          (e) => e.receiveMessageOnPort
        );
      }
    return new NodeSABSyncReceiveMessageTransport();
  }
  constructor() {
  }
  afterResponseSent(e) {
    const { notifyBuffer: r } = e.data;
    if (r) {
      const s = new Int32Array(r);
      s[0] = 1, Atomics.notify(s, 0);
    }
  }
  send(e, r, s) {
    var l;
    const n = new SharedArrayBuffer(4), o = new Int32Array(n);
    o[0] = 0;
    const i = generateUUID();
    if (e.postMessage(
      { ...r, id: i, notifyBuffer: n },
      s
    ), Atomics.wait(o, 0, 0, 5e3) === "timed-out")
      throw new Error("Timeout waiting for response");
    for (; ; ) {
      const d = NodeSABSyncReceiveMessageTransport.receiveMessageOnPort(e);
      if (((l = d.message) == null ? void 0 : l.id) === i)
        return d.message;
      if (!d)
        throw new Error("No response received");
    }
  }
}
/**
 * Original, unmodified Comlink library from Google:
 *
 * @license
 * Copyright 2019 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const proxyMarker = Symbol("Comlink.proxy"), createEndpoint = Symbol("Comlink.endpoint"), releaseProxy = Symbol("Comlink.releaseProxy"), finalizer = Symbol("Comlink.finalizer"), throwMarker = Symbol("Comlink.thrown");
/**
 * @license
 * Copyright 2019 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const WireValueType = {
  RAW: "RAW",
  HANDLER: "HANDLER"
}, MessageType = {
  GET: "GET",
  SET: "SET",
  APPLY: "APPLY",
  CONSTRUCT: "CONSTRUCT",
  ENDPOINT: "ENDPOINT",
  RELEASE: "RELEASE"
}, isObject = (t) => typeof t == "object" && t !== null || typeof t == "function", proxyTransferHandler = {
  canHandle: (t) => isObject(t) && t[proxyMarker],
  serialize(t) {
    const { port1: e, port2: r } = new MessageChannel();
    return expose(t, e), [r, [r]];
  },
  deserialize(t) {
    return t.start(), wrap(t);
  }
}, throwTransferHandler$1 = {
  canHandle: (t) => isObject(t) && throwMarker in t,
  serialize({ value: t }) {
    let e;
    return t instanceof Error ? e = {
      isError: !0,
      value: {
        message: t.message,
        name: t.name,
        stack: t.stack
      }
    } : e = { isError: !1, value: t }, [e, []];
  },
  deserialize(t) {
    throw t.isError ? Object.assign(
      new Error(t.value.message),
      t.value
    ) : t.value;
  }
}, transferHandlers = /* @__PURE__ */ new Map([
  ["proxy", proxyTransferHandler],
  ["throw", throwTransferHandler$1]
]);
function isAllowedOrigin(t, e) {
  for (const r of t)
    if (e === r || r === "*" || r instanceof RegExp && r.test(e))
      return !0;
  return !1;
}
function expose(t, e = globalThis, r = ["*"], s) {
  e.addEventListener("message", function n(o) {
    if (!o || !o.data)
      return;
    if (!isAllowedOrigin(r, o.origin)) {
      console.warn(`Invalid origin '${o.origin}' for comlink proxy`);
      return;
    }
    const { id: i, type: a, path: c } = {
      path: [],
      ...o.data
    }, l = (o.data.argumentList || []).map(fromWireValue);
    let d;
    try {
      const p = c.slice(0, -1).reduce((P, S) => P[S], t), h = c.reduce((P, S) => P[S], t);
      switch (a) {
        case MessageType.GET:
          d = h;
          break;
        case MessageType.SET:
          p[c.slice(-1)[0]] = fromWireValue(
            o.data.value
          ), d = !0;
          break;
        case MessageType.APPLY:
          d = h.apply(p, l);
          break;
        case MessageType.CONSTRUCT:
          {
            const P = new h(...l);
            d = proxy(P);
          }
          break;
        case MessageType.ENDPOINT:
          {
            const { port1: P, port2: S } = new MessageChannel();
            expose(t, S), d = transfer(P, [P]);
          }
          break;
        case MessageType.RELEASE:
          d = void 0;
          break;
        default:
          return;
      }
    } catch (p) {
      d = { value: p, [throwMarker]: 0 };
    }
    Promise.resolve(d).catch((p) => ({ value: p, [throwMarker]: 0 })).then((p) => {
      const [h, P] = toWireValue(p);
      e.postMessage({ ...h, id: i }, P), a === MessageType.RELEASE && (e.removeEventListener("message", n), closeEndPoint(e), finalizer in t && typeof t[finalizer] == "function" && t[finalizer]());
    }).catch(() => {
      const [p, h] = toWireValue({
        value: new TypeError("Unserializable return value"),
        [throwMarker]: 0
      });
      e.postMessage({ ...p, id: i }, h);
    }).finally(() => {
      s == null || s(o);
    });
  }), e.start && e.start();
}
function isMessagePort(t) {
  return t.constructor.name === "MessagePort";
}
function closeEndPoint(t) {
  isMessagePort(t) && t.close();
}
function wrap(t, e) {
  const r = /* @__PURE__ */ new Map();
  return t.addEventListener("message", function(n) {
    const { data: o } = n;
    if (!o || !o.id)
      return;
    const i = r.get(o.id);
    if (i)
      try {
        i(o);
      } finally {
        r.delete(o.id);
      }
  }), createProxy(t, r, [], e);
}
function throwIfProxyReleased(t) {
  if (t)
    throw new Error("Proxy has been released and is not useable");
}
function releaseEndpoint(t) {
  return requestResponseMessage(t, /* @__PURE__ */ new Map(), {
    type: MessageType.RELEASE
  }).then(() => {
    closeEndPoint(t);
  });
}
const proxyCounter = /* @__PURE__ */ new WeakMap(), proxyFinalizers = "FinalizationRegistry" in globalThis && new FinalizationRegistry((t) => {
  const e = (proxyCounter.get(t) || 0) - 1;
  proxyCounter.set(t, e), e === 0 && releaseEndpoint(t);
});
function registerProxy(t, e) {
  const r = (proxyCounter.get(e) || 0) + 1;
  proxyCounter.set(e, r), proxyFinalizers && proxyFinalizers.register(t, e, t);
}
function unregisterProxy(t) {
  proxyFinalizers && proxyFinalizers.unregister(t);
}
function createProxy(t, e, r = [], s = function() {
}) {
  let n = !1;
  const o = new Proxy(s, {
    get(i, a) {
      if (throwIfProxyReleased(n), a === releaseProxy)
        return () => {
          unregisterProxy(o), releaseEndpoint(t), e.clear(), n = !0;
        };
      if (a === "then") {
        if (r.length === 0)
          return { then: () => o };
        const c = requestResponseMessage(t, e, {
          type: MessageType.GET,
          path: r.map((l) => l.toString())
        }).then(fromWireValue);
        return c.then.bind(c);
      }
      return createProxy(t, e, [...r, a]);
    },
    set(i, a, c) {
      throwIfProxyReleased(n);
      const [l, d] = toWireValue(c);
      return requestResponseMessage(
        t,
        e,
        {
          type: MessageType.SET,
          path: [...r, a].map((p) => p.toString()),
          value: l
        },
        d
      ).then(fromWireValue);
    },
    apply(i, a, c) {
      throwIfProxyReleased(n);
      const l = r[r.length - 1];
      if (l === createEndpoint)
        return requestResponseMessage(t, e, {
          type: MessageType.ENDPOINT
        }).then(fromWireValue);
      if (l === "bind")
        return createProxy(t, e, r.slice(0, -1));
      const [d, p] = processArguments(c);
      return requestResponseMessage(
        t,
        e,
        {
          type: MessageType.APPLY,
          path: r.map((h) => h.toString()),
          argumentList: d
        },
        p
      ).then(fromWireValue);
    },
    construct(i, a) {
      throwIfProxyReleased(n);
      const [c, l] = processArguments(a);
      return requestResponseMessage(
        t,
        e,
        {
          type: MessageType.CONSTRUCT,
          path: r.map((d) => d.toString()),
          argumentList: c
        },
        l
      ).then(fromWireValue);
    }
  });
  return registerProxy(o, t), o;
}
function myFlat(t) {
  return Array.prototype.concat.apply([], t);
}
function processArguments(t) {
  const e = t.map(toWireValue);
  return [e.map((r) => r[0]), myFlat(e.map((r) => r[1]))];
}
const transferCache = /* @__PURE__ */ new WeakMap();
function transfer(t, e) {
  return transferCache.set(t, e), t;
}
function proxy(t) {
  return Object.assign(t, { [proxyMarker]: !0 });
}
function windowEndpoint(t, e = globalThis, r = "*") {
  return {
    postMessage: (s, n) => t.postMessage(s, r, n),
    addEventListener: e.addEventListener.bind(e),
    removeEventListener: e.removeEventListener.bind(e)
  };
}
function toWireValue(t) {
  for (const [e, r] of transferHandlers)
    if (r.canHandle(t)) {
      const [s, n] = r.serialize(t);
      return [
        {
          type: WireValueType.HANDLER,
          name: e,
          value: s
        },
        n
      ];
    }
  return [
    {
      type: WireValueType.RAW,
      value: t
    },
    transferCache.get(t) || []
  ];
}
function fromWireValue(t) {
  switch (t.type) {
    case WireValueType.HANDLER:
      return transferHandlers.get(t.name).deserialize(t.value);
    case WireValueType.RAW:
      return t.value;
  }
}
function requestResponseMessage(t, e, r, s) {
  return new Promise((n) => {
    const o = generateUUID();
    e.set(o, n), t.start && t.start(), t.postMessage({ id: o, ...r }, s);
  });
}
function generateUUID() {
  return new Array(4).fill(0).map(
    () => Math.floor(Math.random() * Number.MAX_SAFE_INTEGER).toString(16)
  ).join("-");
}
function nodeEndpoint(t) {
  const e = /* @__PURE__ */ new WeakMap();
  return {
    postMessage: t.postMessage.bind(t),
    addEventListener: (r, s) => {
      const n = (o) => {
        "handleEvent" in s ? s.handleEvent({ data: o }) : s({ data: o });
      };
      t.on("message", n), e.set(s, n);
    },
    removeEventListener: (r, s) => {
      const n = e.get(s);
      n && (t.off("message", n), e.delete(s));
    },
    start: t.start && t.start.bind(t)
  };
}
const proxyByListener = /* @__PURE__ */ new WeakMap();
function nodeProcessEndpoint(t) {
  const e = t || process;
  if (typeof e.send != "function")
    throw new Error(
      "IPC channel is not available. Did you forget to fork the process?"
    );
  const r = e;
  return {
    postMessage(s, n) {
      var o;
      if (n && n.length > 0)
        throw new Error(
          "Transferable objects are not supported for nodeProcessEndpoint"
        );
      (o = r.send) == null || o.call(r, s);
    },
    addEventListener(s, n) {
      const o = typeof n == "function" ? (i) => n({ data: i }) : (i) => n.handleEvent({ data: i });
      proxyByListener.set(n, o), r.addListener(s, o);
    },
    removeEventListener(s, n) {
      const o = proxyByListener.get(n);
      o && (proxyByListener.delete(n), r.removeListener(s, o));
    },
    start() {
    }
  };
}
const list = [
  // Native ES errors https://262.ecma-international.org/12.0/#sec-well-known-intrinsic-objects
  Error,
  EvalError,
  RangeError,
  ReferenceError,
  SyntaxError,
  TypeError,
  URIError,
  AggregateError,
  // Built-in errors
  globalThis.DOMException,
  // Node-specific errors
  // https://nodejs.org/api/errors.html
  globalThis.AssertionError,
  globalThis.SystemError
].filter(Boolean).map((t) => [t.name, t]), errorConstructors = new Map(list);
class NonError extends Error {
  constructor(e) {
    super(NonError._prepareSuperMessage(e)), this.name = "NonError";
  }
  static _prepareSuperMessage(e) {
    try {
      return JSON.stringify(e);
    } catch {
      return String(e);
    }
  }
}
const errorProperties = [
  {
    property: "name",
    enumerable: !1
  },
  {
    property: "message",
    enumerable: !1
  },
  {
    property: "stack",
    enumerable: !1
  },
  {
    property: "code",
    enumerable: !0
  },
  {
    property: "cause",
    enumerable: !1
  },
  {
    property: "errors",
    enumerable: !1
  }
], toJsonWasCalled = /* @__PURE__ */ new WeakSet(), toJSON = (t) => {
  toJsonWasCalled.add(t);
  const e = t.toJSON();
  return toJsonWasCalled.delete(t), e;
}, newError = (t) => {
  const e = errorConstructors.get(t) ?? Error;
  return e === AggregateError ? new e([]) : new e();
}, destroyCircular = ({
  from: t,
  seen: e,
  to: r,
  forceEnumerable: s,
  maxDepth: n,
  depth: o,
  useToJSON: i,
  serialize: a
}) => {
  if (r || (Array.isArray(t) ? r = [] : !a && isErrorLike(t) ? r = newError(t.name) : r = {}), e.push(t), o >= n)
    return r;
  if (i && typeof t.toJSON == "function" && !toJsonWasCalled.has(t))
    return toJSON(t);
  const c = (l) => destroyCircular({
    from: l,
    seen: [...e],
    forceEnumerable: s,
    maxDepth: n,
    depth: o,
    useToJSON: i,
    serialize: a
  });
  for (const [l, d] of Object.entries(t)) {
    if (d && d instanceof Uint8Array && d.constructor.name === "Buffer") {
      r[l] = "[object Buffer]";
      continue;
    }
    if (d !== null && typeof d == "object" && typeof d.pipe == "function") {
      r[l] = "[object Stream]";
      continue;
    }
    if (typeof d != "function") {
      if (!d || typeof d != "object") {
        try {
          r[l] = d;
        } catch {
        }
        continue;
      }
      if (!e.includes(t[l])) {
        o++, r[l] = c(t[l]);
        continue;
      }
      r[l] = "[Circular]";
    }
  }
  if (a || r instanceof Error)
    for (const { property: l, enumerable: d } of errorProperties)
      t[l] !== void 0 && t[l] !== null && Object.defineProperty(r, l, {
        value: isErrorLike(t[l]) || Array.isArray(t[l]) ? c(t[l]) : t[l],
        enumerable: s ? !0 : d,
        configurable: !0,
        writable: !0
      });
  return r;
};
function serializeError(t, e = {}) {
  const { maxDepth: r = Number.POSITIVE_INFINITY, useToJSON: s = !0 } = e;
  return typeof t == "object" && t !== null ? destroyCircular({
    from: t,
    seen: [],
    forceEnumerable: !0,
    maxDepth: r,
    depth: 0,
    useToJSON: s,
    serialize: !0
  }) : typeof t == "function" ? `[Function: ${t.name || "anonymous"}]` : t;
}
function deserializeError(t, e = {}) {
  const { maxDepth: r = Number.POSITIVE_INFINITY } = e;
  return t instanceof Error ? t : isMinimumViableSerializedError(t) ? destroyCircular({
    from: t,
    seen: [],
    to: newError(t.name),
    maxDepth: r,
    depth: 0,
    serialize: !1
  }) : new NonError(t);
}
function isErrorLike(t) {
  return !!t && typeof t == "object" && typeof t.name == "string" && typeof t.message == "string" && typeof t.stack == "string";
}
function isMinimumViableSerializedError(t) {
  return !!t && typeof t == "object" && typeof t.message == "string" && !Array.isArray(t);
}
const releaseApiProxy = releaseProxy;
async function consumeAPISync(t) {
  setupTransferHandlers();
  const e = await NodeSABSyncReceiveMessageTransport.create();
  return wrapSync(t, e);
}
function consumeAPI(t, e = void 0) {
  setupTransferHandlers();
  let r;
  if (typeof process < "u" && typeof process.versions < "u" && typeof process.versions.node < "u")
    if ("postMessage" in t)
      r = nodeEndpoint(t);
    else if ("send" in t && "addListener" in t)
      r = nodeProcessEndpoint(t);
    else
      throw new Error(
        "consumeAPI: remote does not look like a Worker, MessagePort, or Process"
      );
  else if (t instanceof Worker)
    r = t;
  else {
    const i = windowEndpoint(
      t,
      e
    ), a = wrap(i);
    r = deferredEndpoint(
      connectWindowApiThroughMessagePort(a)
    );
  }
  const n = wrap(r), o = proxyClone(n);
  return new Proxy(o, {
    get: (i, a) => a === "isConnected" ? async () => {
      for (; ; )
        try {
          await runWithTimeout(n.isConnected(), 200);
          break;
        } catch {
        }
    } : n[a]
  });
}
async function connectWindowApiThroughMessagePort(t) {
  for (; ; )
    try {
      return await runWithTimeout(t.isConnected(), 200), await t[createEndpoint]();
    } catch {
    }
}
function deferredEndpoint(t) {
  let e, r = !1;
  const s = [], n = [];
  return t.then((o) => {
    e = o;
    for (const { type: i, listener: a, options: c } of s)
      e.addEventListener(i, a, c);
    r && e.start();
    for (const { message: i, transfer: a } of n)
      a ? e.postMessage(i, a) : e.postMessage(i);
    n.length = 0;
  }), {
    postMessage(o, i) {
      e ? i ? e.postMessage(o, i) : e.postMessage(o) : n.push({ message: o, transfer: i });
    },
    addEventListener(o, i, a) {
      e ? e.addEventListener(o, i, a) : s.push({ type: o, listener: i, options: a });
    },
    removeEventListener(o, i, a) {
      if (e) {
        e.removeEventListener(o, i, a);
        return;
      }
      const c = s.findIndex(
        (l) => l.type === o && l.listener === i && l.options === a
      );
      c !== -1 && s.splice(c, 1);
    },
    start() {
      e ? e.start() : r = !0;
    }
  };
}
async function runWithTimeout(t, e) {
  return new Promise((r, s) => {
    const n = setTimeout(s, e);
    t.then(
      (o) => {
        clearTimeout(n), r(o);
      },
      (o) => {
        clearTimeout(n), s(o);
      }
    );
  });
}
function exposeAPI(t, e, r) {
  const { setReady: s, setFailed: n, exposedApi: o } = prepareForExpose(
    t,
    e
  );
  let i;
  if (r)
    if ("addEventListener" in r)
      i = r;
    else if ("postMessage" in r)
      i = nodeEndpoint(r);
    else if ("send" in r && "addListener" in r)
      i = nodeProcessEndpoint(r);
    else
      throw new Error(
        "exposeAPI: targetWorker does not look like a Worker, MessagePort, or Process"
      );
  else
    i = typeof window < "u" ? windowEndpoint(self.parent) : void 0;
  return expose(o, i), [s, n, o];
}
async function exposeSyncAPI(t, e) {
  const { setReady: r, setFailed: s, exposedApi: n } = prepareForExpose(t), o = await NodeSABSyncReceiveMessageTransport.create(), i = nodeEndpoint(e);
  return exposeSync(n, i, o), [r, s, n];
}
function prepareForExpose(t, e) {
  setupTransferHandlers();
  const r = Promise.resolve();
  let s, n;
  const o = new Promise((c, l) => {
    s = c, n = l;
  }), i = proxyClone(t), a = new Proxy(i, {
    get: (c, l) => l === "isConnected" ? () => r : l === "isReady" ? () => o : l in c ? c[l] : e == null ? void 0 : e[l]
  });
  return { setReady: s, setFailed: n, exposedApi: a };
}
let isTransferHandlersSetup = !1;
function setupTransferHandlers() {
  if (isTransferHandlersSetup)
    return;
  isTransferHandlersSetup = !0, transferHandlers.set("EVENT", {
    canHandle: (n) => n instanceof CustomEvent,
    serialize: (n) => [
      {
        detail: n.detail
      },
      []
    ],
    deserialize: (n) => n
  }), transferHandlers.set("FUNCTION", {
    // eslint-disable-next-line @typescript-eslint/no-unsafe-function-type
    canHandle: (n) => typeof n == "function",
    // eslint-disable-next-line @typescript-eslint/no-unsafe-function-type
    serialize(n) {
      const { port1: o, port2: i } = new MessageChannel();
      return expose(n, o), [i, [i]];
    },
    deserialize(n) {
      return n.start(), wrap(n);
    }
  }), transferHandlers.set("MESSAGE_PORT", {
    canHandle: (n) => n instanceof MessagePort,
    serialize(n) {
      return [n, [n]];
    },
    deserialize(n) {
      return n;
    }
  });
  const t = {
    canHandle: (n) => typeof ReadableStream < "u" && n instanceof ReadableStream,
    serialize(n) {
      if (supportsTransferableStreams())
        return [{ stream: n }, [n]];
      const o = streamToPort(n);
      return [{ port: o }, [o]];
    },
    deserialize(n) {
      return n.stream || portToStream(n.port);
    }
  };
  transferHandlers.set(
    "READABLE_STREAM",
    t
  );
  const e = {
    canHandle: (n) => typeof n == "object" && n !== null && phpEventStdinTransfer in n && n[phpEventStdinTransfer] === !0 && "type" in n && typeof n.type == "string" && "stdin" in n && t.canHandle(n.stdin),
    serialize(n) {
      const [o, i] = t.serialize(n.stdin);
      return [{ ...n, stdin: o }, i];
    },
    deserialize(n) {
      return {
        ...n,
        stdin: t.deserialize(n.stdin),
        [phpEventStdinTransfer]: !0
      };
    }
  };
  transferHandlers.set(
    "EVENT_WITH_READABLE_STDIN",
    e
  ), transferHandlers.set("PHPResponse", {
    canHandle: (n) => typeof n == "object" && n !== null && "headers" in n && "bytes" in n && "errors" in n && "exitCode" in n && "httpStatusCode" in n,
    serialize(n) {
      const o = n.toRawData(), i = [];
      return o.bytes.buffer.byteLength > 0 && i.push(o.bytes.buffer), [o, i];
    },
    deserialize(n) {
      return PHPResponse.fromRawData(n);
    }
  });
  const r = transferHandlers.get("throw"), s = r == null ? void 0 : r.serialize;
  r.serialize = ({ value: n }) => {
    const o = s({ value: n });
    return n.response && (o[0].value.response = n.response), n.source && (o[0].value.source = n.source), o;
  }, transferHandlers.set("StreamedPHPResponse", {
    canHandle: (n) => n instanceof StreamedPHPResponse,
    serialize(n) {
      const o = supportsTransferableStreams(), i = promiseToPort(n.exitCode), a = n.getHeadersStream();
      if (o)
        return [
          {
            __type: "StreamedPHPResponse",
            headers: a,
            stdout: n.stdout,
            stderr: n.stderr,
            exitCodePort: i
          },
          [
            a,
            n.stdout,
            n.stderr,
            i
          ]
        ];
      const c = streamToPort(a), l = streamToPort(n.stdout), d = streamToPort(n.stderr);
      return [
        {
          __type: "StreamedPHPResponse",
          headersPort: c,
          stdoutPort: l,
          stderrPort: d,
          exitCodePort: i
        },
        [c, l, d, i]
      ];
    },
    deserialize(n) {
      if (n.headers && n.stdout && n.stderr) {
        const l = portToPromise(
          n.exitCodePort
        );
        return new StreamedPHPResponse(
          n.headers,
          n.stdout,
          n.stderr,
          l
        );
      }
      const o = portToStream(n.headersPort), i = portToStream(n.stdoutPort), a = portToStream(n.stderrPort), c = portToPromise(n.exitCodePort);
      return new StreamedPHPResponse(o, i, a, c);
    }
  });
}
let _cachedSupportsTransferableStreams;
function supportsTransferableStreams() {
  if (typeof ReadableStream > "u" && (_cachedSupportsTransferableStreams = !1), _cachedSupportsTransferableStreams === void 0)
    try {
      const { port1: t } = new MessageChannel(), e = new ReadableStream();
      t.postMessage(e, [e]);
      try {
        t.close();
      } catch {
      }
      _cachedSupportsTransferableStreams = !0;
    } catch {
      _cachedSupportsTransferableStreams = !1;
    }
  return _cachedSupportsTransferableStreams;
}
function streamToPort(t) {
  const { port1: e, port2: r } = new MessageChannel(), s = t.getReader(), n = (o) => {
    var i;
    ((i = o.data) == null ? void 0 : i.t) === "cancel" && s.cancel().catch(() => {
    });
  };
  return e.addEventListener("message", n), e.start(), (async () => {
    try {
      for (; ; ) {
        const { done: o, value: i } = await s.read();
        if (o) {
          try {
            e.postMessage({ t: "close" });
          } catch {
          }
          try {
            e.close();
          } catch {
          }
          break;
        }
        if (i) {
          const a = i.slice(), c = a.buffer;
          try {
            e.postMessage({ t: "chunk", b: c }, [
              c
            ]);
          } catch {
            e.postMessage({
              t: "chunk",
              b: a.buffer.slice(0)
            });
          }
        }
      }
    } catch (o) {
      try {
        e.postMessage({ t: "error", m: (o == null ? void 0 : o.message) || String(o) });
      } catch {
      }
    } finally {
      e.removeEventListener("message", n);
      try {
        e.close();
      } catch {
      }
    }
  })(), r;
}
function portToStream(t) {
  return new ReadableStream({
    start(e) {
      const r = (n) => {
        const o = n.data;
        if (o)
          switch (o.t) {
            case "chunk":
              try {
                e.enqueue(new Uint8Array(o.b));
              } catch {
                s();
              }
              break;
            case "close":
              safeStreamClose(e), s();
              break;
            case "error":
              safeStreamError(
                e,
                new Error(o.m || "Stream error")
              ), s();
              break;
          }
      }, s = () => {
        var n;
        try {
          (n = t.removeEventListener) == null || n.call(t, "message", r);
        } catch {
        }
        try {
          t.onmessage = null;
        } catch {
        }
        try {
          t.close();
        } catch {
        }
      };
      t.addEventListener ? t.addEventListener("message", r) : t.on ? t.on(
        "message",
        (n) => r({ data: n })
      ) : t.onmessage = r, typeof t.start == "function" && t.start();
    },
    cancel() {
      try {
        t.postMessage({ t: "cancel" });
      } catch {
      }
      try {
        t.close();
      } catch {
      }
    }
  });
}
function promiseToPort(t) {
  const { port1: e, port2: r } = new MessageChannel();
  return t.then((s) => {
    try {
      e.postMessage({ t: "resolve", v: s });
    } catch {
    }
  }).catch((s) => {
    try {
      e.postMessage({
        t: "reject",
        m: (s == null ? void 0 : s.message) || String(s)
      });
    } catch {
    }
  }).finally(() => {
    try {
      e.close();
    } catch {
    }
  }), r;
}
function portToPromise(t) {
  return new Promise((e, r) => {
    const s = (o) => {
      const i = o.data;
      i && (i.t === "resolve" ? (n(), e(i.v)) : i.t === "reject" && (n(), r(new Error(i.m || ""))));
    }, n = () => {
      var o;
      try {
        (o = t.removeEventListener) == null || o.call(t, "message", s);
      } catch {
      }
      try {
        t.onmessage = null;
      } catch {
      }
      try {
        t.close();
      } catch {
      }
    };
    t.addEventListener ? t.addEventListener("message", s) : t.on ? t.on(
      "message",
      (o) => s({ data: o })
    ) : t.onmessage = s, typeof t.start == "function" && t.start();
  });
}
const throwTransferHandler = transferHandlers.get(
  "throw"
), throwTransferHandlerCustom = {
  canHandle: throwTransferHandler.canHandle,
  serialize: ({ value: t }) => {
    let e;
    return t instanceof Error ? (e = {
      isError: !0,
      value: serializeError(t)
    }, e.value.originalErrorClassName = t.constructor.name) : e = { isError: !1, value: t }, [e, []];
  },
  deserialize: (t) => {
    if (t.isError) {
      const e = deserializeError(t.value), r = new Error("Comlink method call failed");
      let s = e;
      for (; s.cause; )
        s = s.cause;
      throw s.cause = r, e;
    }
    throw t.value;
  }
};
transferHandlers.set("throw", throwTransferHandlerCustom);
function proxyClone(t) {
  return new Proxy(t, {
    get(e, r) {
      switch (typeof e[r]) {
        case "function":
          return (...s) => e[r](...s);
        case "object":
          return e[r] === null ? e[r] : proxyClone(e[r]);
        case "undefined":
        case "number":
        case "string":
          return e[r];
        default:
          return proxy(e[r]);
      }
    }
  });
}
function safeStreamError(t, e) {
  try {
    t.error(e);
  } catch {
  }
}
function safeStreamClose(t) {
  try {
    t.close();
  } catch {
  }
}
const MAX_ADDRESSABLE_FILE_OFFSET = BigInt(Number.MAX_SAFE_INTEGER);
class IntervalNode {
  constructor(e) {
    this.left = null, this.right = null, this.range = e, this.max = e.end;
  }
}
class FileLockIntervalTree {
  constructor() {
    this.root = null;
  }
  isEmpty() {
    return this.root === null;
  }
  /**
   * Insert a new locked range into the tree
   */
  insert(e) {
    this.root = this.insertNode(this.root, e);
  }
  /**
   * Find all ranges that overlap with the given range
   */
  findOverlapping(e) {
    const r = [];
    return this.findOverlappingRanges(this.root, e, r), r;
  }
  /**
   * Remove a lock range from the tree
   */
  remove(e) {
    this.root = this.removeNode(this.root, e);
  }
  /**
   * Find all ranges locked by the given process.
   *
   * @param pid The process ID to find locks for.
   * @returns All locked ranges for the given process.
   */
  findLocksForProcess(e) {
    const r = [];
    return this.findLocksForProcessInNode(this.root, e, r), r;
  }
  /**
   * Find the strictest existing lock type in the range lock tree.
   *
   * @returns The strictest existing lock type, or 'unlocked' if no locks exist.
   */
  findStrictestExistingLockType() {
    let e = "unlocked";
    const r = (s) => {
      if (s) {
        if (s.range.type === "exclusive") {
          e = "exclusive";
          return;
        }
        s.range.type === "shared" && (e = "shared"), r(s.left), r(s.right);
      }
    };
    return r(this.root), e;
  }
  insertNode(e, r) {
    return e ? (r.start < e.range.start ? e.left = this.insertNode(e.left, r) : e.right = this.insertNode(e.right, r), e.max = this.bigintMax(e.max, r.end), e) : new IntervalNode(r);
  }
  bigintMax(...e) {
    return e.reduce((r, s) => s > r ? s : r, e[0]);
  }
  findOverlappingRanges(e, r, s) {
    e && (this.doRangesOverlap(e.range, r) && s.push(e.range), e.left && e.left.max >= r.start && this.findOverlappingRanges(e.left, r, s), e.right && e.range.start <= r.end && this.findOverlappingRanges(e.right, r, s));
  }
  doRangesOverlap(e, r) {
    return e.start < r.end && r.start < e.end;
  }
  removeNode(e, r) {
    if (!e)
      return null;
    if (this.areRangesEqual(e.range, r)) {
      if (!e.left)
        return e.right;
      if (!e.right)
        return e.left;
      const s = this.findMin(e.right);
      e.range = s.range, e.right = this.removeNode(e.right, s.range);
    } else r.start < e.range.start ? e.left = this.removeNode(e.left, r) : e.right = this.removeNode(e.right, r);
    return e.max = e.range.end, e.left && (e.max = this.bigintMax(e.max, e.left.max)), e.right && (e.max = this.bigintMax(e.max, e.right.max)), e;
  }
  findMin(e) {
    let r = e;
    for (; r.left; )
      r = r.left;
    return r;
  }
  areRangesEqual(e, r) {
    return e.start === r.start && e.end === r.end && e.pid === r.pid && e.fd === r.fd;
  }
  findLocksForProcessInNode(e, r, s) {
    e && (e.range.pid === r && s.push(e.range), this.findLocksForProcessInNode(e.left, r, s), this.findLocksForProcessInNode(e.right, r, s));
  }
}
class FileLockManagerInMemory {
  /**
   * Create a new FileLockManagerInMemory instance.
   *
   * @param nativeFlockSync A synchronous flock() function to lock files via the host OS.
   */
  constructor() {
    this.locks = /* @__PURE__ */ new Map();
  }
  /**
   * Lock the whole file.
   *
   * @param path The path to the file to lock. This should be the path
   *             of the file in the native filesystem.
   * @param op The whole file lock operation to perform.
   * @returns True if the lock was granted, false otherwise.
   */
  lockWholeFile(e, r) {
    if (this.locks.get(e) === void 0) {
      if (r.type === "unlock")
        return !0;
      this.locks.set(e, new FileLock());
    }
    const n = this.locks.get(e).lockWholeFile(r);
    return this.forgetPathIfUnlocked(e), n;
  }
  /**
   * Lock a byte range.
   *
   * @param path The path to the file to lock. This should be the path
   *             of the file in the native filesystem.
   * @param requestedLock The byte range lock to perform.
   * @returns True if the lock was granted, false otherwise.
   */
  lockFileByteRange(e, r) {
    if (!this.locks.has(e)) {
      if (r.type === "unlocked")
        return !0;
      this.locks.set(e, new FileLock());
    }
    return this.locks.get(e).lockFileByteRange(r);
  }
  /**
   * Find the first conflicting byte range lock.
   *
   * @param path The path to the file to find the conflicting lock for.
   * @param desiredLock The desired byte range lock.
   * @returns The first conflicting byte range lock, or undefined if no conflicting lock exists.
   */
  findFirstConflictingByteRangeLock(e, r) {
    const s = this.locks.get(e);
    if (s !== void 0)
      return s.findFirstConflictingByteRangeLock(r);
  }
  /**
   * Release all locks for the given process.
   *
   * @param pid The process ID to release locks for.
   */
  releaseLocksForProcess(e) {
    for (const [r, s] of this.locks.entries())
      s.releaseLocksForProcess(e), this.forgetPathIfUnlocked(r);
  }
  /**
   * Release all locks for the given process and file descriptor.
   *
   * @param pid The process ID to release locks for.
   * @param fd The file descriptor to release locks for.
   * @param path The path to the file to release locks for.
   */
  releaseLocksOnFdClose(e, r, s) {
    const n = this.locks.get(s);
    n && (n.releaseLocksOnFdClose(e, r), this.forgetPathIfUnlocked(s));
  }
  /**
   * Forget the path if it is unlocked.
   *
   * @param path The path to the file to forget.
   */
  forgetPathIfUnlocked(e) {
    const r = this.locks.get(e);
    r && r.isUnlocked() && this.locks.delete(e);
  }
}
class FileLock {
  constructor() {
    this.rangeLocks = new FileLockIntervalTree(), this.wholeFileLock = { type: "unlocked" };
  }
  /**
   * Lock the whole file.
   *
   * This method corresponds to the flock() function.
   *
   * @param op The whole file lock operation to perform.
   * @returns True if the lock was granted, false otherwise.
   */
  lockWholeFile(e) {
    if (e.type === "unlock")
      return this.wholeFileLock.type === "unlocked" || (this.wholeFileLock.type === "exclusive" && this.wholeFileLock.pid === e.pid && this.wholeFileLock.fd === e.fd ? this.wholeFileLock = { type: "unlocked" } : this.wholeFileLock.type === "shared" && this.wholeFileLock.pidFds.has(e.pid) && this.wholeFileLock.pidFds.get(e.pid).has(e.fd) && (this.wholeFileLock.pidFds.get(e.pid).delete(e.fd), this.wholeFileLock.pidFds.get(e.pid).size === 0 && this.wholeFileLock.pidFds.delete(e.pid), this.wholeFileLock.pidFds.size === 0 && (this.wholeFileLock = { type: "unlocked" }))), !0;
    if (this.isThereAConflictWithRequestedWholeFileLock(e))
      return !1;
    if (e.type === "exclusive")
      return this.wholeFileLock = {
        type: "exclusive",
        pid: e.pid,
        fd: e.fd
      }, !0;
    if (e.type === "shared") {
      this.wholeFileLock.type !== "shared" && (this.wholeFileLock = {
        type: "shared",
        pidFds: /* @__PURE__ */ new Map()
      });
      const r = this.wholeFileLock;
      return r.pidFds.has(e.pid) || r.pidFds.set(e.pid, /* @__PURE__ */ new Set()), r.pidFds.get(e.pid).add(e.fd), !0;
    }
    throw new Error(`Unexpected wholeFileLock() op: '${e.type}'`);
  }
  /**
   * Lock a byte range.
   *
   * This method corresponds to the fcntl() F_SETLK command.
   *
   * @param requestedLock The byte range lock to perform.
   * @returns True if the lock was granted, false otherwise.
   */
  lockFileByteRange(e) {
    if (e.start === e.end && (e = {
      ...e,
      end: MAX_ADDRESSABLE_FILE_OFFSET
    }), e.type === "unlocked") {
      const i = this.rangeLocks.findOverlapping(e).filter((a) => a.pid === e.pid);
      for (const a of i)
        this.rangeLocks.remove(a), a.start < e.start && this.rangeLocks.insert({
          ...a,
          end: e.start
        }), a.end > e.end && this.rangeLocks.insert({
          ...a,
          start: e.end
        });
      return !0;
    }
    if (this.isThereAConflictWithRequestedRangeLock(e))
      return !1;
    const r = this.rangeLocks.findOverlapping(e).filter((i) => i.pid === e.pid);
    let s = e.start, n = e.end;
    for (const i of r)
      this.rangeLocks.remove(i), i.start < s && (s = i.start), i.end > n && (n = i.end);
    const o = {
      ...e,
      start: s,
      end: n
    };
    return this.rangeLocks.insert(o), !0;
  }
  /**
   * Find the first conflicting byte range lock.
   *
   * This method corresponds to the fcntl() F_GETLK command.
   *
   * @param desiredLock The desired byte range lock.
   * @returns The first conflicting byte range lock, or undefined if no conflicting lock exists.
   */
  findFirstConflictingByteRangeLock(e) {
    e.start === e.end && (e = {
      ...e,
      end: MAX_ADDRESSABLE_FILE_OFFSET
    });
    const s = this.rangeLocks.findOverlapping(e).find(
      (o) => o.pid !== e.pid && (e.type === "exclusive" || o.type === "exclusive")
    );
    if (s)
      return s;
    if (this.wholeFileLock.type === "unlocked")
      return;
    if (this.wholeFileLock.type === "exclusive" || e.type === "exclusive")
      return {
        type: this.wholeFileLock.type,
        start: 0n,
        end: 0n,
        pid: -1
      };
  }
  /**
   * Release all locks for the given process.
   *
   * @param pid The process ID to release locks for.
   */
  releaseLocksForProcess(e) {
    for (const r of this.rangeLocks.findLocksForProcess(e))
      this.lockFileByteRange({
        ...r,
        type: "unlocked"
      });
    if (this.wholeFileLock.type === "exclusive" && this.wholeFileLock.pid === e)
      this.lockWholeFile({
        pid: e,
        fd: this.wholeFileLock.fd,
        type: "unlock"
      });
    else if (this.wholeFileLock.type === "shared" && this.wholeFileLock.pidFds.has(e))
      for (const r of this.wholeFileLock.pidFds.get(e))
        this.lockWholeFile({
          pid: e,
          fd: r,
          type: "unlock"
        });
  }
  /**
   * Release all locks for the given process and file descriptor.
   *
   * @param pid The process ID to release locks for.
   * @param fd The file descriptor to release locks for.
   */
  releaseLocksOnFdClose(e, r) {
    for (const s of this.rangeLocks.findLocksForProcess(e))
      this.lockFileByteRange({
        ...s,
        type: "unlocked"
      });
    this.lockWholeFile({
      pid: e,
      fd: r,
      type: "unlock"
    });
  }
  /**
   * Check if the file lock is unlocked.
   *
   * @returns True if the file lock is unlocked, false otherwise.
   */
  isUnlocked() {
    return this.wholeFileLock.type === "unlocked" && this.rangeLocks.isEmpty();
  }
  /**
   * Check if a lock exists that conflicts with the requested range lock.
   *
   * @param requestedLock The desired byte range lock.
   * @returns True if a conflicting lock exists, false otherwise.
   */
  isThereAConflictWithRequestedRangeLock(e) {
    return this.findFirstConflictingByteRangeLock(e) !== void 0;
  }
  /**
   * Check if a lock exists that conflicts with the requested whole-file lock.
   *
   * @param requestedLock The desired whole-file lock.
   * @returns True if a conflicting lock exists, false otherwise.
   */
  isThereAConflictWithRequestedWholeFileLock(e) {
    return e.type === "exclusive" ? !!(this.wholeFileLock.type === "exclusive" && (this.wholeFileLock.fd !== e.fd || this.wholeFileLock.pid !== e.pid) || this.wholeFileLock.type === "shared" && Array.from(this.wholeFileLock.pidFds).some(
      ([s]) => s !== e.pid
    ) || this.rangeLocks.findOverlapping({
      start: 0n,
      end: MAX_ADDRESSABLE_FILE_OFFSET
    }).length > 0) : e.type === "shared" ? this.wholeFileLock.type === "exclusive" && this.wholeFileLock.pid !== e.pid || this.rangeLocks.findOverlapping({
      start: 0n,
      end: MAX_ADDRESSABLE_FILE_OFFSET
    }).filter(
      (n) => n.type === "exclusive"
    ).length > 0 : !1;
  }
}
class FileLockManagerComposite {
  constructor({
    nativeLockManager: e,
    wasmLockManager: r
  }) {
    this.nativeLockManager = e, this.wasmLockManager = r;
  }
  lockWholeFile(e, r) {
    if (r.type !== "unlock") {
      let s, n;
      try {
        if (s = this.nativeLockManager.lockWholeFile(e, r), !s)
          return !1;
        n = this.wasmLockManager.lockWholeFile(e, r);
      } catch (o) {
        logger.error("Unexpected error in lockWholeFile()", o);
      } finally {
        s && !n && this.nativeLockManager.lockWholeFile(e, {
          ...r,
          type: "unlock"
        });
      }
      return !!s && !!n;
    }
    try {
      this.wasmLockManager.lockWholeFile(e, r);
    } catch (s) {
      logger.error(
        "Unexpected error unlocking whole file with in-memory lock manager",
        s
      );
    }
    try {
      this.nativeLockManager.lockWholeFile(e, r);
    } catch (s) {
      logger.error(
        "Unexpected error unlocking whole file with native lock manager",
        s
      );
    }
    return !0;
  }
  lockFileByteRange(e, r, s) {
    if (r.type !== "unlocked") {
      let n, o;
      try {
        if (n = this.nativeLockManager.lockFileByteRange(
          e,
          r,
          s
        ), !n)
          return !1;
        o = this.wasmLockManager.lockFileByteRange(
          e,
          r,
          s
        );
      } catch (i) {
        logger.error("Unexpected error in lockFileByteRange()", i);
      } finally {
        n && !o && this.nativeLockManager.lockFileByteRange(
          e,
          {
            ...r,
            type: "unlocked"
          },
          !1
        );
      }
      return !!n && !!o;
    }
    try {
      this.wasmLockManager.lockFileByteRange(
        e,
        r,
        s
      );
    } catch (n) {
      logger.error(
        "Unexpected error unlocking byte range with in-memory lock manager",
        n
      );
    }
    try {
      this.nativeLockManager.lockFileByteRange(
        e,
        r,
        s
      );
    } catch (n) {
      logger.error(
        "Unexpected error unlocking byte range with native lock manager",
        n
      );
    }
    return !0;
  }
  findFirstConflictingByteRangeLock(e, r) {
    try {
      const s = this.nativeLockManager.findFirstConflictingByteRangeLock(
        e,
        r
      );
      return s || this.wasmLockManager.findFirstConflictingByteRangeLock(
        e,
        r
      );
    } catch (s) {
      logger.error(
        "Unexpected error in findFirstConflictingByteRangeLock()",
        s
      );
      return;
    }
  }
  releaseLocksForProcess(e) {
    try {
      this.wasmLockManager.releaseLocksForProcess(e);
    } catch (r) {
      logger.error(
        "Unexpected error in wasmLockManager.releaseLocksForProcess()",
        r
      );
    }
    try {
      this.nativeLockManager.releaseLocksForProcess(e);
    } catch (r) {
      logger.error(
        "Unexpected error in nativeLockManager.releaseLocksForProcess()",
        r
      );
    }
  }
  releaseLocksOnFdClose(e, r, s) {
    try {
      this.wasmLockManager.releaseLocksOnFdClose(e, r, s);
    } catch (n) {
      logger.error(
        "Unexpected error in wasmLockManager.releaseLocksOnFdClose()",
        n
      );
    }
    try {
      this.nativeLockManager.releaseLocksOnFdClose(e, r, s);
    } catch (n) {
      logger.error(
        "Unexpected error in nativeLockManager.releaseLocksOnFdClose()",
        n
      );
    }
  }
}
function createObjectPoolProxy(t) {
  if (t.length === 0)
    throw new Error("At least one instance is required");
  const e = [...t], r = [];
  function s() {
    const i = e.shift();
    return i !== void 0 ? Promise.resolve(i) : new Promise((a) => {
      r.push(a);
    });
  }
  function n(i) {
    const a = r.shift();
    a ? a(i) : e.push(i);
  }
  function o(i) {
    return s().then((a) => {
      const c = (d) => {
        const p = d == null ? void 0 : d.finished;
        return p && typeof p.then == "function" ? Promise.resolve(p).then(
          () => n(a),
          () => n(a)
        ) : n(a), d;
      };
      let l;
      try {
        l = i(a);
      } catch (d) {
        throw n(a), d;
      }
      return l != null && typeof l.then == "function" ? l.then(
        (d) => c(d),
        (d) => {
          throw n(a), d;
        }
      ) : c(l);
    });
  }
  return new Proxy({}, {
    get(i, a) {
      if (a in i)
        return i[a];
      if (a !== "then")
        return new Proxy(function() {
        }, {
          apply(c, l, d) {
            return o((p) => p[a](...d));
          },
          get(c, l) {
            if (l === "then")
              return (d, p) => o((h) => h[a]).then(
                d,
                p
              );
          }
        });
    }
  });
}
const maxValueForSigned32BitInteger = 2 ** 31 - 1;
class ProcessIdAllocator {
  constructor(e = 1, r = maxValueForSigned32BitInteger) {
    this.claimed = /* @__PURE__ */ new Set(), this.initialId = e, this.maxId = r, this.nextId = e;
  }
  claim() {
    const e = this.maxId - this.initialId + 1;
    for (let r = 0; r < e; r++)
      if (this.claimed.has(this.nextId))
        this.nextId++, this.nextId > this.maxId && (this.nextId = this.initialId);
      else
        return this.claimed.add(this.nextId), this.nextId;
    throw new Error(
      `Unable to find free process ID after ${e} tries.`
    );
  }
  release(e) {
    return this.claimed.has(e) ? (this.claimed.delete(e), !0) : !1;
  }
}
export {
  AllPHPVersions,
  DEFAULT_BASE_URL,
  FSHelpers,
  FileLock,
  FileLockIntervalTree,
  FileLockManagerComposite,
  FileLockManagerInMemory,
  HttpCookieStore,
  LEGACY_PHP_INI_CONTENT,
  LEGACY_PHP_INI_PATH,
  LatestSupportedPHPVersion,
  LegacyPHPVersions,
  MAX_ADDRESSABLE_FILE_OFFSET,
  MountStillActiveError,
  PHP,
  PHPExecutionFailureError,
  PHPNextVersion,
  PHPProcessManager,
  PHPRequestHandler,
  PHPResponse,
  PHPWorker,
  PHP_EXTENSIONS_DIR,
  ProcessIdAllocator,
  SinglePHPInstanceManager,
  StreamedPHPResponse,
  SupportedPHPVersions,
  SupportedPHPVersionsList,
  UnhandledRejectionsTarget,
  __private__dont__use,
  applyRewriteRules,
  consumeAPI,
  consumeAPISync,
  createLegacyPhpIniPreRunStep,
  createObjectPoolProxy,
  describeError,
  ensurePathPrefix,
  exposeAPI,
  exposeSyncAPI,
  getPhpIniEntries,
  inferMimeType,
  installPHPExtensionFilesSync,
  isExitCode,
  isLegacyPHPVersion,
  isPHPNextVersion,
  isPathToSharedFS,
  iteratePhpFiles as iterateFiles,
  loadPHPRuntime,
  Ge as phpEventStdinTransfer,
  popLoadedRuntime,
  portToStream,
  prettyPrintFullStackTrace,
  printDebugDetails,
  printResponseDebugDetails,
  proxyFileSystem,
  releaseApiProxy,
  removePathPrefix,
  resolvePHPExtension,
  rotatePHPRuntime,
  sandboxedSpawnHandlerFactory,
  setPhpIniEntries,
  streamToPort,
  toRelativeUrl,
  withPHPIniValues,
  withResolvedPHPExtensions,
  writeFiles,
  writeFilesStreamToPhp
};
//# sourceMappingURL=index.js.map
